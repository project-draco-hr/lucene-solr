{
synchronized (cores) {
    CoreDescriptor dcore=cores.get(name);
    if (dcore == null)     throw new SolrException(SolrException.ErrorCode.BAD_REQUEST,"No such core: " + name);
    create(new CoreDescriptor(dcore));
  }
}
