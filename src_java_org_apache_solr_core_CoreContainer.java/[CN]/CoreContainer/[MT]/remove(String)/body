{
synchronized (cores) {
    CoreDescriptor dcore=cores.remove(name);
    if (dcore == null) {
      return;
    }
    SolrCore core=dcore.getCore();
    if (core != null) {
      core.close();
    }
  }
}
