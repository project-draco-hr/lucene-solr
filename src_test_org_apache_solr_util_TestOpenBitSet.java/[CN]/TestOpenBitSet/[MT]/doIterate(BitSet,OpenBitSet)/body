{
  int aa=-1, bb=-1;
  BitSetIterator iterator=new BitSetIterator(b);
  do {
    aa=a.nextSetBit(aa + 1);
    if (rand.nextBoolean())     bb=iterator.next();
 else     bb=iterator.next(bb + 1);
    assertEquals(aa,bb);
  }
 while (aa >= 0);
}
