{
  if (!f.canRead()) {
    return;
  }
  if (f.isDirectory()) {
    File files[]=f.listFiles();
    for (int i=0; i < files.length; i++) {
      addFiles(files[i]);
    }
    return;
  }
  txtFiles.add(f);
  addUniqueBytes(f.length());
}
