{
  String line;
synchronized (this) {
    while (true) {
      line=fileIn.readLine();
      if (line == null) {
        openFile();
        if (!forever)         throw new NoMoreDataException();
      }
 else {
        break;
      }
    }
  }
  return getDocState().setFields(line);
}
