{
  try {
    System.clearProperty("solr.solrxml.location");
    System.setProperty("hostPort","8787");
    setUpZkAndDiskXml(false,false);
    assertEquals("Should have gotten the default port from the hard-coded default solr.xml file via sys prop.",cfg.getZkHostPort(),"8787");
  }
  finally {
    closeZK();
  }
}
