{
  if (words == null) {
    return input;
  }
 else {
    @SuppressWarnings("deprecation") final TokenStream filter=new KeepWordFilter(luceneMatchVersion,enablePositionIncrements,input,words);
    return filter;
  }
}
