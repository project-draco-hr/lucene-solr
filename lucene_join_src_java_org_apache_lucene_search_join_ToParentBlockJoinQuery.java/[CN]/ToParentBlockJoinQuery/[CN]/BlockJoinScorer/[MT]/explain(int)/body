{
  int start=prevParentDoc + 1;
  if (acceptDocs != null) {
    while (acceptDocs.get(start) == false) {
      start++;
    }
  }
  int end=parentDoc - 1;
  return new ComplexExplanation(true,score(),String.format(Locale.ROOT,"Score based on child doc range from %d to %d",docBase + start,docBase + end));
}
