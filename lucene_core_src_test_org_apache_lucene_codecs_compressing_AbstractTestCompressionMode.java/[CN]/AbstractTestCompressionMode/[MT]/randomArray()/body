{
  final int max=random().nextBoolean() ? random().nextInt(4) : random().nextInt(255);
  final int length=random().nextBoolean() ? random().nextInt(20) : random().nextInt(192 * 1024);
  return randomArray(length,max);
}
