{
  Analyzer a=new GalicianAnalyzer(TEST_VERSION_CURRENT);
  checkOneTermReuse(a,"correspondente","correspond");
  checkOneTermReuse(a,"corresponder??","correspond");
  assertAnalyzesTo(a,"e",new String[]{});
}
