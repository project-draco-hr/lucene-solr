{
  if (size == 0)   return null;
  if (enumerator != null && enumerator.term() != null && position >= enumerator.position && position < (enumerator.position + TermInfosWriter.INDEX_INTERVAL))   return scanEnum(position);
  seekEnum(position / TermInfosWriter.INDEX_INTERVAL);
  return scanEnum(position);
}
