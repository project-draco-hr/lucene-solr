{
  if (size == 0)   return null;
  if (enum != null && enum.term() != null && position >= enum.position && position < (enum.position + TermInfosWriter.INDEX_INTERVAL))   return scanEnum(position);
  seekEnum(position / TermInfosWriter.INDEX_INTERVAL);
  return scanEnum(position);
}
