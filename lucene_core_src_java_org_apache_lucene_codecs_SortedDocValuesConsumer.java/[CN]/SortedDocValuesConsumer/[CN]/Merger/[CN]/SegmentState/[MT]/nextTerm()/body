{
  while (ord < source.getValueCount()) {
    ord++;
    if (liveTerms == null || liveTerms.get(ord)) {
      source.getByOrd(ord,scratch);
      return scratch;
    }
 else {
      ord++;
    }
  }
  return null;
}
