{
  Integer docCountToKeep=this.action.get(doc);
  if (!doc.isDelete() && (docCountToKeep == null || docCountToKeep == 0)) {
    this.action.put(doc,ONE);
  }
 else   if (doc.isDelete() && (docCountToKeep == null || docCountToKeep > 0)) {
    this.action.put(doc,ZERO);
  }
}
