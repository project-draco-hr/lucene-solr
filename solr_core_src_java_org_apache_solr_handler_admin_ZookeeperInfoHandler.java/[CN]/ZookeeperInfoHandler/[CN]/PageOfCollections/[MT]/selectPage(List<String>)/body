{
  numFound=collections.size();
  selected=collections;
  if (rows > 0) {
    if (start > numFound)     start=0;
    int lastIndex=Math.min(start + rows,numFound);
    if (start > 0 || lastIndex < numFound)     selected=collections.subList(start,lastIndex);
  }
}
