{
  Directory dir=getRunData().getDirectory();
  IndexReader r=null;
  if (userData == null)   r=IndexReader.open(dir,true);
 else   r=IndexReader.open(OpenReaderTask.findIndexCommit(dir,userData),null,true);
  System.out.println("--> numDocs:" + r.numDocs() + " dels:"+ r.numDeletedDocs());
  r.close();
  return 1;
}
