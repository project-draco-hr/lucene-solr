{
  if (br == null) {
    return "(null)";
  }
 else {
    try {
      return br.utf8ToString() + " " + br.toString();
    }
 catch (    IllegalArgumentException t) {
      return br.toString();
    }
  }
}
