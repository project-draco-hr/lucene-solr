{
  Info info=fields.get(fieldName);
  if (info == null) {
    return 0;
  }
  BytesRef[] values=info.pointValues;
  if (values != null) {
    return info.pointValuesCount;
  }
 else {
    return 0;
  }
}
