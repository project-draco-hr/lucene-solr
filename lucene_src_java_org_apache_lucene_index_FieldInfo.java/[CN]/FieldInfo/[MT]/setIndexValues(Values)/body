{
  if (indexValues != null) {
    if (indexValues != v) {
      throw new IllegalArgumentException("indexValues is already set to " + indexValues + "; cannot change to "+ v);
    }
  }
 else {
    indexValues=v;
  }
}
