{
  if (positions.isPayloadAvailable()) {
    payload=positions.getPayload(payload,0);
    payloadScore=function.currentScore(doc,term.field(),payloadsSeen,payloadScore,similarity.scorePayload(doc,term.field(),payload,0,positions.getPayloadLength()));
    payloadsSeen++;
  }
 else {
  }
}
