{
  try {
    r=new Random(base + range + seed);
    for (int i=0; i < iterations; i++) {
      indexDoc();
    }
  }
 catch (  Exception e) {
    e.printStackTrace();
    TestCase.fail(e.toString());
  }
synchronized (this) {
    docs.size();
  }
}
