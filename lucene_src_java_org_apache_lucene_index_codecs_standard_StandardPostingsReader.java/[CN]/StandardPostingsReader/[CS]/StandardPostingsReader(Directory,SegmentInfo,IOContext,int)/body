{
  freqIn=dir.openInput(IndexFileNames.segmentFileName(segmentInfo.name,codecId,StandardCodec.FREQ_EXTENSION),context);
  if (segmentInfo.getHasProx()) {
    boolean success=false;
    try {
      proxIn=dir.openInput(IndexFileNames.segmentFileName(segmentInfo.name,codecId,StandardCodec.PROX_EXTENSION),context);
      success=true;
    }
  finally {
      if (!success) {
        freqIn.close();
      }
    }
  }
 else {
    proxIn=null;
  }
}
