{
  super.setUp();
  final String docText[]={"docThatNeverMatchesSoWeCanRequireLastDocCollectedToBeGreaterThanZero","one blah three","one foo three multiOne","one foobar three multiThree","blueberry pancakes","blueberry pie","blueberry strudel","blueberry pizza"};
  directory=new RAMDirectory();
  RandomIndexWriter iw=new RandomIndexWriter(newRandom(),directory,new IndexWriterConfig(TEST_VERSION_CURRENT,new MockAnalyzer()));
  for (int i=0; i < N_DOCS; i++) {
    add(docText[i % docText.length],iw);
  }
  reader=iw.getReader();
  iw.close();
  searcher=new IndexSearcher(reader);
  String qtxt="one";
  for (int i=1; i < docText.length; i++) {
    qtxt+=' ' + docText[i];
  }
  QueryParser queryParser=new QueryParser(TEST_VERSION_CURRENT,FIELD_NAME,new MockAnalyzer());
  query=queryParser.parse(qtxt);
  searcher.search(query,null,1000);
}
