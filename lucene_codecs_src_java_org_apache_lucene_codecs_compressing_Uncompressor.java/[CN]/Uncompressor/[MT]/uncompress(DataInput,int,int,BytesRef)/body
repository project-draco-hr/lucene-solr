{
  uncompress(in,bytes);
  if (bytes.length < offset + length) {
    throw new IndexOutOfBoundsException((offset + length) + " > " + bytes.length);
  }
  bytes.offset+=offset;
  bytes.length=length;
}
