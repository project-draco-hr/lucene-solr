{
  dictionary=new HighFrequencyDictionary(reader,field,threshold);
  FieldType fieldType=schema.getFieldTypeNoEx(field);
  analyzer=fieldType == null ? new WhitespaceAnalyzer() : fieldType.getQueryAnalyzer();
}
