import types
import os
import sys
import random
MAX_UNICODE = 1114111
utf8Ranges = [(0, 127), (128, 2047), (2048, 65535), (65536, 1114111)]
typeToColor = {'startend': 'purple', 'start': 'blue', 'end': 'red', }
MASKS = []
v = 2
for i in range(32):
    MASKS.append((v - 1))
    v *= 2
if (__name__ == '__main__'):
    if (len(sys.argv) > 1):
        main()
    else:
        stress()
