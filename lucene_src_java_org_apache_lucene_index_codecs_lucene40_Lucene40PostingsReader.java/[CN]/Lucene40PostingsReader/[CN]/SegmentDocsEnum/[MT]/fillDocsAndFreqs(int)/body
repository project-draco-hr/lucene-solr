{
  int docs[]=this.docs;
  int freqs[]=this.freqs;
  for (int i=0; i < size; i++) {
    int code=freqIn.readVInt();
    accum+=code >>> 1;
    docs[i]=accum;
    if ((code & 1) != 0) {
      freqs[i]=1;
    }
 else {
      freqs[i]=freqIn.readVInt();
    }
  }
}
