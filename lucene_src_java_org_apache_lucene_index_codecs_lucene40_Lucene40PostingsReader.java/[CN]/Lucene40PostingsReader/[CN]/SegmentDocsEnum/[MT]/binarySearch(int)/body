{
  int hi=count - 1;
  while (start <= hi) {
    int mid=(hi + start) >>> 1;
    int doc=docs[mid];
    if (doc < target) {
      start=mid + 1;
    }
 else     if (doc > target) {
      hi=mid - 1;
    }
 else {
      start=mid;
      break;
    }
  }
  start--;
}
