{
  StringBuilder buffer=new StringBuilder();
  buffer.append("<expr \"");
  buffer.append(getField());
  buffer.append("\">");
  if (getReverse()) {
    buffer.append('!');
  }
  return buffer.toString();
}
