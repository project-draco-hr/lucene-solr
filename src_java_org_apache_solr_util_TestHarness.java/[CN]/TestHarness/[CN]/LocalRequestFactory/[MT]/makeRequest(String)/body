{
  if (q.length == 1) {
    return new LocalSolrQueryRequest(TestHarness.this.getCore(),q[0],qtype,start,limit,args);
  }
  return new LocalSolrQueryRequest(TestHarness.this.getCore(),new NamedList(Arrays.asList(q)));
}
