{
  for (int i=0; i < 2; i++) {
    new IndexWriter(dir,new WhitespaceAnalyzer(Version.LUCENE_CURRENT),new DeleteLastCommitPolicy(),MaxFieldLength.UNLIMITED).close();
    IndexReader r=IndexReader.open(dir,true);
    assertEquals(100,r.numDocs());
    r.close();
  }
}
