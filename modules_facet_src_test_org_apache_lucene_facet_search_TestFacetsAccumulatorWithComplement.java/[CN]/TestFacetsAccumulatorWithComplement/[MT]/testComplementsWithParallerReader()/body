{
  IndexReader origReader=indexReader;
  ParallelReader pr=new ParallelReader(true);
  pr.add(origReader);
  indexReader=pr;
  try {
    doTestComplements();
  }
  finally {
    indexReader=origReader;
  }
}
