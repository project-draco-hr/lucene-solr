{
  IndexReader origReader=indexReader;
  ParallelAtomicReader pr=new ParallelAtomicReader.Builder(true).add(SlowCompositeReaderWrapper.wrap(origReader)).build();
  indexReader=pr;
  try {
    doTestComplements();
  }
  finally {
    indexReader=origReader;
  }
}
