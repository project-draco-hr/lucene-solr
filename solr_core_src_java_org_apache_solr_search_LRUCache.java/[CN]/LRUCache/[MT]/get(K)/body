{
synchronized (map) {
    V val=map.get(key);
    if (getState() == State.LIVE) {
      lookups++;
      stats.lookups.increment();
      if (val != null) {
        hits++;
        stats.hits.increment();
      }
    }
    return val;
  }
}
