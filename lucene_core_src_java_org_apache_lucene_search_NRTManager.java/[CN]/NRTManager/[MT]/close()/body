{
  reopenLock.lock();
  try {
    try {
      searchingGen=MAX_SEARCHER_GEN;
      mgr.close();
    }
  finally {
      newGeneration.signalAll();
    }
  }
  finally {
    reopenLock.unlock();
  }
}
