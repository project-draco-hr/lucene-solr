{
  String nm=attrName.toString();
  if (atomize)   nm=Atom.getAtom(nm);
  String val=isBoolean ? BOOLATTR : (rcgnzEntities ? entMngr.entityDecode(attrValue) : attrValue).toString();
  attrName.reset();
  attrValue.reset();
  attrs.put(nm,val);
}
