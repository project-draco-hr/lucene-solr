{
  int sz=val.size();
  indent();
  writer.write('[');
  incLevel();
  for (int i=0; i < sz; i++) {
    if (i != 0) {
      writer.write(',');
    }
    String key=val.getName(i);
    indent();
    writeStr(null,key,true);
    writer.write(',');
    writeVal(key,val.getVal(i));
  }
  decLevel();
  writer.write(']');
}
