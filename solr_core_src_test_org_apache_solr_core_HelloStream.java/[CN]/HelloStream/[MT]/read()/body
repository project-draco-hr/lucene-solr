{
  if (isSentHelloWorld) {
    Map m=new HashMap();
    m.put("EOF",true);
    return new Tuple(m);
  }
 else {
    isSentHelloWorld=true;
    Map m=new HashMap<>();
    m.put("msg","Hello World!");
    return new Tuple(m);
  }
}
