{
  if (term == null) {
    throw new UnsupportedOperationException("MultiTermQuerys that do not use a pattern term need to override equals/hashcode");
  }
  return term.hashCode() + Float.floatToRawIntBits(getBoost());
}
