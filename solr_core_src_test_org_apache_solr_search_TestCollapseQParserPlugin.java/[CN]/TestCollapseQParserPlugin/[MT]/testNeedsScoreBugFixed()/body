{
  String[] doc={"id","1","group_s","xyz","text_ws","hello xxx world"};
  assertU(adoc(doc));
  assertU(commit());
  ModifiableSolrParams params=params("q","{!surround df=text_ws} 2W(hello, world)","fq","{!collapse field=group_s}","facet","true","facet.field","group_s");
  assertQ(req(params));
  assertQ(req(params));
}
