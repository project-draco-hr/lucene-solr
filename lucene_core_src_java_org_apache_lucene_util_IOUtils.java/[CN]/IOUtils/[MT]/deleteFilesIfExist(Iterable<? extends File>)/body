{
  Throwable th=null;
  for (  File file : files) {
    try {
      if (file != null) {
        Files.deleteIfExists(file.toPath());
      }
    }
 catch (    Throwable t) {
      addSuppressed(th,t);
      if (th == null) {
        th=t;
      }
    }
  }
  reThrow(th);
}
