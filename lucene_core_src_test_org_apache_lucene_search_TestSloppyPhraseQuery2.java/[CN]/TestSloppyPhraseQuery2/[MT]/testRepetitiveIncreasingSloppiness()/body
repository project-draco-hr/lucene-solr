{
  Term t=randomTerm();
  PhraseQuery q1=new PhraseQuery();
  q1.add(t);
  q1.add(t);
  PhraseQuery q2=new PhraseQuery();
  q2.add(t);
  q2.add(t);
  for (int i=0; i < 10; i++) {
    q1.setSlop(i);
    q2.setSlop(i + 1);
    assertSubsetOf(q1,q2);
  }
}
