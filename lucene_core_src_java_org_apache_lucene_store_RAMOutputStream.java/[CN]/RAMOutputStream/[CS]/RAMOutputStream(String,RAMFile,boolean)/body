{
  super("RAMOutputStream(name=\"" + name + "\")");
  file=f;
  currentBufferIndex=-1;
  currentBuffer=null;
  if (checksum) {
    crc=new BufferedChecksum(new CRC32());
  }
 else {
    crc=null;
  }
}
