{
  if (size() < myMaxSize) {
    add(element);
    return null;
  }
 else   if (size() > 0 && !lessThan(element,heap[1])) {
    CacheEntry<K,V> ret=heap[1];
    heap[1]=element;
    updateTop();
    return ret;
  }
 else {
    return element;
  }
}
