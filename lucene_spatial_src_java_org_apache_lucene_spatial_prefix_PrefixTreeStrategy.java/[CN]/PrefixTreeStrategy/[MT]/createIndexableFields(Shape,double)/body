{
  int detailLevel=grid.getLevelForDistance(distErr);
  List<Cell> cells=grid.getCells(shape,detailLevel,true,simplifyIndexedCells);
  Field field=new Field(getFieldName(),new CellTokenStream().setCells(cells.iterator()),FIELD_TYPE);
  return new Field[]{field};
}
