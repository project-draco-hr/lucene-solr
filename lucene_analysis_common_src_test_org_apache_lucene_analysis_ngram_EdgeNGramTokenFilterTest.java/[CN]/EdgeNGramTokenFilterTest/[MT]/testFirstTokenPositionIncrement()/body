{
  TokenStream ts=whitespaceMockTokenizer("a abc");
  ts=new PositionFilter(ts);
  EdgeNGramTokenFilter filter=new EdgeNGramTokenFilter(ts,2,3);
  assertTokenStreamContents(filter,new String[]{"ab","abc"},new int[]{2,2},new int[]{5,5},new int[]{1,0});
}
