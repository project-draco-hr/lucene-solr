{
  if (this == obj) {
    return true;
  }
  if (!super.equals(obj)) {
    return false;
  }
  TermsQuery that=(TermsQuery)obj;
  return termDataHashCode == that.termDataHashCode && termData.equals(that.termData);
}
