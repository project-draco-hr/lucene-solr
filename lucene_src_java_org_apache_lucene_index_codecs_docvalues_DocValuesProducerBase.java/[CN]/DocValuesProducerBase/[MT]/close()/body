{
  Collection<DocValues> values=docValues.values();
  IOException ex=null;
  for (  DocValues docValues : values) {
    try {
      docValues.close();
    }
 catch (    IOException e) {
      ex=e;
    }
  }
  if (ex != null) {
    throw ex;
  }
}
