{
  long ord=values.lookupTerm(text);
  if (ord >= 0) {
    term.offset=0;
    term.bytes=new byte[text.length];
    term.copyBytes(text);
    currentOrd=ord;
    return true;
  }
 else {
    return false;
  }
}
