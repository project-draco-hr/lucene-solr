{
  int sz=val.size();
  startTag("arr",name,sz <= 0);
  incLevel();
  for (  Object o : val) {
    writeVal(null,o);
  }
  decLevel();
  if (sz > 0) {
    if (doIndent)     indent();
    writer.write("</arr>");
  }
}
