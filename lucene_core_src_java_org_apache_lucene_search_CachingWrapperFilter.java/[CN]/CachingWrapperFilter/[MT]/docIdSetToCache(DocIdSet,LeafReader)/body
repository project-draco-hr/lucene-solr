{
  if (docIdSet == null) {
    return EMPTY;
  }
 else   if (docIdSet.isCacheable()) {
    return docIdSet;
  }
 else {
    final DocIdSetIterator it=docIdSet.iterator();
    if (it == null) {
      return EMPTY;
    }
 else {
      return cacheImpl(it,reader);
    }
  }
}
