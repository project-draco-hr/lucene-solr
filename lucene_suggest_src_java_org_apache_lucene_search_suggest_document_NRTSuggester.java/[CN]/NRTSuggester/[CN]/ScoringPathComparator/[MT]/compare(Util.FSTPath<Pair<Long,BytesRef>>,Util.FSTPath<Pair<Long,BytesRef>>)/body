{
  int cmp=Float.compare(scorer.score(decode(second.cost.output1),second.boost),scorer.score(decode(first.cost.output1),first.boost));
  return (cmp != 0) ? cmp : first.input.get().compareTo(second.input.get());
}
