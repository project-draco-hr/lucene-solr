{
  if (this.readLock.tryLock()) {
    try {
      if (this.pool.size() < this.size && !this.isDestroyed)       this.pool.push(type);
 else       this.factory.destroyInstance(type);
    }
  finally {
      this.readLock.unlock();
    }
    return;
  }
  this.factory.destroyInstance(type);
}
