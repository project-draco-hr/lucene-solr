{
  final int len=1 + this.nextInt(10);
  final char[] buffer=new char[len + 1];
  for (int i=0; i < len; i++) {
    buffer[i]=(char)this.nextInt(97,123);
  }
  buffer[len]=0xffff;
  return buffer;
}
