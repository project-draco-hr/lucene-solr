{
  for (  final Membership bound : bounds) {
    if (!bound.isWithin(p))     return false;
  }
  for (  final Membership bound : moreBounds) {
    if (!bound.isWithin(p))     return false;
  }
  return true;
}
