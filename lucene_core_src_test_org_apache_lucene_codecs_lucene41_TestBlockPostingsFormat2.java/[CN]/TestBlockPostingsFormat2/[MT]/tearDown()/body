{
  iw.close();
  _TestUtil.checkIndex(dir);
  iwc.setOpenMode(OpenMode.APPEND);
  IndexWriter iw=new IndexWriter(dir,iwc.clone());
  iw.forceMerge(1);
  iw.close();
  dir.close();
  super.tearDown();
}
