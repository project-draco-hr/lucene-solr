{
  boolean success=false;
  try {
    _mergeInit(merge);
    success=true;
  }
  finally {
    if (!success) {
      if (infoStream != null) {
        infoStream.message("IW","hit exception in mergeInit");
      }
      mergeFinish(merge);
    }
  }
}
