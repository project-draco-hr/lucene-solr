{
  QueryParserWrapper qp=new QueryParserWrapper("field",new MockAnalyzer(MockTokenizer.WHITESPACE,false));
  Query query1=qp.parse("A AND B OR C AND D");
  Query query2=qp.parse("+A +B +C +D");
  assertEquals(query1,query2);
}
