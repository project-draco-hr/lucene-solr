{
  TermInfo ti;
  Term term;
  if (segmentTermEnum.fieldInfos == fieldInfos) {
    term=segmentTermEnum.term();
    ti=segmentTermEnum.termInfo();
  }
 else {
    term=segmentTermEnum.term();
    ti=tis.get(term);
  }
  seek(ti,term);
}
