{
  int i=0;
  while (i < length && count < df) {
    doc+=freqStream.readVInt();
    count++;
    if (liveDocs == null || liveDocs.get(doc)) {
      docs[i]=doc;
      freqs[i]=1;
      ++i;
    }
  }
  return i;
}
