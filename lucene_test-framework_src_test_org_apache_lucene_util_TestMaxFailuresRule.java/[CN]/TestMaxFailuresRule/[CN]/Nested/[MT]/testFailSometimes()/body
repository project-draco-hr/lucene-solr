{
  numIters++;
  boolean fail=random().nextInt(5) == 0;
  if (fail)   numFails++;
  if (numFails < DESIRED_FAILURES && DESIRED_FAILURES <= TOTAL_ITERS - numIters) {
    fail=true;
  }
  assertFalse(fail);
}
