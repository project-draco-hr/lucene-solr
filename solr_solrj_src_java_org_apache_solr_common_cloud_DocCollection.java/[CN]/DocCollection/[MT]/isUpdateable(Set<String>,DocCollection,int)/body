{
  int updateableShards=0;
  for (  Slice slice : collectionState) {
    Replica leader=slice.getLeader();
    if (leader != null && leader.isActive(liveNodes))     updateableShards++;
  }
  return updateableShards == expectedShards;
}
