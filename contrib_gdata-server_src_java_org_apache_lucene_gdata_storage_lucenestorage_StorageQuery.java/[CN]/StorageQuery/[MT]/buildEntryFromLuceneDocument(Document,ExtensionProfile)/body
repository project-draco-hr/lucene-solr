{
  StringReader reader=new StringReader(doc.getField(StorageEntryWrapper.FIELD_CONTENT).stringValue());
  return GDataEntityBuilder.buildEntry(doc.getField(StorageEntryWrapper.FIELD_FEED_ID).stringValue(),reader,profil);
}
