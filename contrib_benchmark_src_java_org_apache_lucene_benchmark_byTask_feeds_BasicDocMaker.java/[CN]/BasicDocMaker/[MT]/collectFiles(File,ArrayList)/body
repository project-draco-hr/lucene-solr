{
  if (!f.canRead()) {
    return;
  }
  if (f.isDirectory()) {
    String files[]=f.list();
    Arrays.sort(files);
    for (int i=0; i < files.length; i++) {
      collectFiles(new File(f,files[i]),inputFiles);
    }
    return;
  }
  inputFiles.add(f);
  addUniqueBytes(f.length());
}
