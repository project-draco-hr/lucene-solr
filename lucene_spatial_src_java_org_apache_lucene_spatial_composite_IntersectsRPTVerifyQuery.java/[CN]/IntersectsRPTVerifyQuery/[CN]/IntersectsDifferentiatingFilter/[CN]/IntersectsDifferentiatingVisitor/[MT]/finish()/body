{
  exactDocIdSet=exactBuilder.build();
  if (approxBuilder.isDefinitelyEmpty()) {
    approxDocIdSet=exactDocIdSet;
  }
 else {
    if (exactDocIdSet != null) {
      approxBuilder.or(exactDocIdSet.iterator());
    }
    approxDocIdSet=approxBuilder.build();
  }
  return null;
}
