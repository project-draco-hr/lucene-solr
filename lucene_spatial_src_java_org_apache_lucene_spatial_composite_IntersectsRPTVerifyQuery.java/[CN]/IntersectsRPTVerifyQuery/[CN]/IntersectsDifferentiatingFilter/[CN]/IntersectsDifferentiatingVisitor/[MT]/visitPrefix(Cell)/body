{
  if (cell.getShapeRel() == SpatialRelation.WITHIN) {
    collectDocs(exactBuilder);
    return false;
  }
 else   if (cell.getLevel() == detailLevel) {
    collectDocs(approxBuilder);
    return false;
  }
  return true;
}
