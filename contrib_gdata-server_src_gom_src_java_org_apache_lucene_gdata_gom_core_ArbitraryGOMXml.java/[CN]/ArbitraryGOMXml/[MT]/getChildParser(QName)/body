{
  if (aName == null)   throw new GDataParseException("QName must not be null");
  if (this.textValue != null)   throw new GDataParseException(String.format(AtomParser.UNEXPECTED_ELEMENT_CHILD,this.localName));
  GOMElement element=new ArbitraryGOMXml(aName);
  this.children.add(element);
  return element;
}
