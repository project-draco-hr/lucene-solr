{
  int format=in.readInt();
  if (format > FORMAT_VERSION2) {
    throw new CorruptIndexException("Incompatible format version: " + format + " expected "+ FORMAT_VERSION2+ " or less");
  }
  return format;
}
