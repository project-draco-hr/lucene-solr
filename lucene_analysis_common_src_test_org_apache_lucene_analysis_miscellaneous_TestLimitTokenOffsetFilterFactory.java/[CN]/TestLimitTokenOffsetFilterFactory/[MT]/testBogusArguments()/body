{
  try {
    tokenFilterFactory("LimitTokenOffset",LimitTokenOffsetFilterFactory.MAX_START_OFFSET,"3","bogusArg","bogusValue");
    fail();
  }
 catch (  IllegalArgumentException expected) {
    assertTrue(expected.getMessage().contains("Unknown parameters"));
  }
}
