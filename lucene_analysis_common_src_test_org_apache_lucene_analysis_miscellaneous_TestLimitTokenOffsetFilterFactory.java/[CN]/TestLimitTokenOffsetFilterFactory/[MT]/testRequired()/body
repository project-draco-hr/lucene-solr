{
  try {
    tokenFilterFactory("LimitTokenOffset");
    fail();
  }
 catch (  IllegalArgumentException e) {
    assertTrue("exception doesn't mention param: " + e.getMessage(),0 < e.getMessage().indexOf(LimitTokenOffsetFilterFactory.MAX_START_OFFSET));
  }
}
