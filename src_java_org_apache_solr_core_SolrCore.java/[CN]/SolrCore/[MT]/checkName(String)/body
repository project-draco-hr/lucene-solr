{
  if (name != null)   for (int i=0, length=name.length(); i < length; ++i) {
    char c=name.charAt(i);
    if (c == '/' || c == '\\' || Character.isSpaceChar(c))     throw new SolrException(SolrException.ErrorCode.SERVER_ERROR,"Invalid core name '" + name + "'");
  }
  return name;
}
