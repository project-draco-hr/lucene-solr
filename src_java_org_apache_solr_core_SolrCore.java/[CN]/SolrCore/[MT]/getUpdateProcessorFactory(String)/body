{
  UpdateRequestProcessorFactory factory=updateProcessors.get(name);
  if (factory == null) {
    throw new SolrException(SolrException.ErrorCode.BAD_REQUEST,"unknown UpdateProcessorFactory: " + name);
  }
  return factory;
}
