{
  if (hasChanges) {
    doCommit();
  }
  termVectorsLocal.close();
  fieldsReaderLocal.close();
  if (liveDocs != null) {
    liveDocsRef.decrementAndGet();
    liveDocs=null;
  }
  if (core != null) {
    core.decRef();
  }
}
