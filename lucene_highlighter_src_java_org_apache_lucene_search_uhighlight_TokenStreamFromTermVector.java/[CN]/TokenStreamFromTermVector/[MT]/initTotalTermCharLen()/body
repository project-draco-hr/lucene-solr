{
  int guessNumTerms;
  if (vector.size() != -1) {
    guessNumTerms=(int)vector.size();
  }
 else   if (offsetLength != -1) {
    guessNumTerms=(int)(offsetLength * 0.33);
  }
 else {
    return 128;
  }
  return Math.max(64,(int)(guessNumTerms * loadFactor * 7.0));
}
