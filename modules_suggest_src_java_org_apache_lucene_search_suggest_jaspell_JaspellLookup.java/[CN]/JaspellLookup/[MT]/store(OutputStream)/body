{
  TSTNode root=trie.getRoot();
  if (root == null) {
    return false;
  }
  DataOutputStream out=new DataOutputStream(output);
  try {
    writeRecursively(out,root);
    out.flush();
  }
  finally {
    IOUtils.close(out);
  }
  return true;
}
