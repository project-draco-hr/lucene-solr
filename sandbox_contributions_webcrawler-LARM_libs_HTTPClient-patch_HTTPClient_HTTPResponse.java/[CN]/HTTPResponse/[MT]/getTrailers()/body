{
  if (got_trailers) {
    return;
  }
  if (!initialized) {
    handleResponse();
  }
  response.getTrailer("Any");
  Trailers=response.Trailers;
  got_trailers=true;
  invokeTrailerHandlers(false);
}
