def check_url_list(lst):
    ret = []
    for url in lst:
        if mirror_contains_file(url):
            p('.')
        else:
            p(((('\nFAIL: ' + url) + '\n') if args.details else 'X'))
            ret.append(url)
    return ret
