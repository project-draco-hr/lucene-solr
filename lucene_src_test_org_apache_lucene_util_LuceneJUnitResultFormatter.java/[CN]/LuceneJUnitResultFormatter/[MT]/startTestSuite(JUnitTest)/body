{
  if (out == null) {
    return;
  }
  try {
    LogManager.getLogManager().readConfiguration();
  }
 catch (  Exception e) {
  }
  sb.setLength(0);
  sb.append("Testsuite: ");
  sb.append(suite.getName());
  sb.append(StringUtils.LINE_SEP);
}
