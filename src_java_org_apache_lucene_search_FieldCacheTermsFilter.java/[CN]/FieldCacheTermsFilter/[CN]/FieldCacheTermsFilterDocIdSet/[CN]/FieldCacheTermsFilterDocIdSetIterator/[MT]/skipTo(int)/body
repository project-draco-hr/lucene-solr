{
  try {
    doc=target;
    while (!openBitSet.fastGet(fcsi.order[doc])) {
      doc++;
    }
    return true;
  }
 catch (  ArrayIndexOutOfBoundsException e) {
    doc=Integer.MAX_VALUE;
    return false;
  }
}
