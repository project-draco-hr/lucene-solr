{
  try {
    do {
      doc++;
    }
 while (!openBitSet.fastGet(fcsi.order[doc]));
    return true;
  }
 catch (  ArrayIndexOutOfBoundsException e) {
    doc=Integer.MAX_VALUE;
    return false;
  }
}
