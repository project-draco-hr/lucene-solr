{
  ServiceFactory serviceFactory=ServiceFactory.getInstance();
  Service service=serviceFactory.getService();
  if (service == null)   throw new ServletException("Service not available");
  return service;
}
