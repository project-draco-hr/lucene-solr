{
  try {
    GOMNamespace namespace=new GOMNamespace(null,"a");
    fail("uri is null");
  }
 catch (  IllegalArgumentException e) {
  }
  try {
    new GOMNamespace("a",null);
  }
 catch (  Exception e) {
    fail("unexp. exc");
  }
  GOMNamespace namespace=new GOMNamespace(GOMNamespace.ATOM_NS_URI,GOMNamespace.ATOM_NS_PREFIX);
  assertEquals(GOMNamespace.ATOM_NS_PREFIX,namespace.getNamespacePrefix());
  assertEquals(GOMNamespace.ATOM_NS_URI,namespace.getNamespaceUri());
}
