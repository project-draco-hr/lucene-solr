{
  if ((_other instanceof SimpleFacetResult) == false) {
    return false;
  }
  SimpleFacetResult other=(SimpleFacetResult)_other;
  return path.equals(other.path) && value.equals(other.value) && Arrays.equals(labelValues,other.labelValues);
}
