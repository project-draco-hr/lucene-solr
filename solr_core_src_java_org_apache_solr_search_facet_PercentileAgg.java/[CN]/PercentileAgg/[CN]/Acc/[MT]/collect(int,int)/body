{
  if (!values.exists(doc))   return;
  double val=values.doubleVal(doc);
  AVLTreeDigest digest=digests[slotNum];
  if (digest == null) {
    digests[slotNum]=digest=new AVLTreeDigest(100);
  }
  digest.add(val);
}
