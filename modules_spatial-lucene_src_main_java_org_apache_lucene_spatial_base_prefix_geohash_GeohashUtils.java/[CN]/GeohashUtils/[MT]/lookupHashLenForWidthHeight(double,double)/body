{
  for (int len=1; len <= MAX_PRECISION; len++) {
    double latHeight=hashLenToLatHeight[len];
    double lonWidth=hashLenToLonWidth[len];
    if (latHeight < height || lonWidth < width)     return len - 1;
  }
  return MAX_PRECISION;
}
