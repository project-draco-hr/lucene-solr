{
  if (solrStreams != null) {
    for (    TupleStream solrStream : solrStreams) {
      solrStream.close();
    }
  }
  if ((this.streamContext == null || this.streamContext.getSolrClientCache() == null) && cloudSolrClient != null) {
    cloudSolrClient.close();
  }
}
