{
  int count=input.read(buffer);
  if (done)   return null;
 else {
    done=true;
    if (count == 1) {
      reusableToken.termBuffer()[0]=buffer[0];
      reusableToken.setTermLength(1);
    }
 else     reusableToken.setTermLength(0);
    return reusableToken;
  }
}
