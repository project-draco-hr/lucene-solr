{
  Object o=super.next();
  if (o != null) {
    this.numRecords++;
    if (isReplaying) {
synchronized (CdcrTransactionLog.this) {
        CdcrTransactionLog.this.numRecords=this.numRecords;
      }
    }
  }
  return o;
}
