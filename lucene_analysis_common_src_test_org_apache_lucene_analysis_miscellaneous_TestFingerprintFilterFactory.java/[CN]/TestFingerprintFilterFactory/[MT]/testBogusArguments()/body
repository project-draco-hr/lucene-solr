{
  try {
    tokenFilterFactory("Fingerprint",FingerprintFilterFactory.MAX_OUTPUT_TOKEN_SIZE_KEY,"3","bogusArg","bogusValue");
    fail();
  }
 catch (  IllegalArgumentException expected) {
    assertTrue(expected.getMessage().contains("Unknown parameters"));
  }
}
