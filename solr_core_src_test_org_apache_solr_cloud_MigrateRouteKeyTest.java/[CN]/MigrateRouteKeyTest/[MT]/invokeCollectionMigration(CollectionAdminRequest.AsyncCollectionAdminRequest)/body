{
  if (random().nextBoolean()) {
    cluster.getSolrClient().setSoTimeout(60000);
    request.process(cluster.getSolrClient());
  }
 else {
    request.processAndWait(cluster.getSolrClient(),60000);
  }
}
