{
  this.prev=0;
  int index=baseHash[baseHashIndex];
  while (index != 0) {
    if (keys[index].equals(key)) {
      return index;
    }
    prev=index;
    index=next[index];
  }
  this.prev=0;
  return 0;
}
