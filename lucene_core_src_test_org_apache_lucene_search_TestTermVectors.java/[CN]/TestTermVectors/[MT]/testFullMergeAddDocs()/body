{
  Directory target=newDirectory();
  IndexWriter writer=createWriter(target);
  for (int i=0; i < 4; i++) {
    writer.addDocument(createDoc());
  }
  writer.forceMerge(1);
  writer.shutdown();
  verifyIndex(target);
  target.close();
}
