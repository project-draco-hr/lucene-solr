{
  if (!w.isClosed()) {
    LuceneTestCase.maybeChangeLiveIndexWriterConfig(r,w.getConfig());
  }
  if (getReaderCalled == false && r.nextInt(8) == 2) {
    doRandomForceMerge();
    w.commit();
  }
  w.close();
}
