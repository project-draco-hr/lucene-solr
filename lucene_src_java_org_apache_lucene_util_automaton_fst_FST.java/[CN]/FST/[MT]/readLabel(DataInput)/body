{
  final int v;
  if (inputType == INPUT_TYPE.BYTE1) {
    v=in.readByte() & 0xFF;
  }
 else   if (inputType == INPUT_TYPE.BYTE2) {
    v=in.readVInt();
  }
 else {
    v=in.readVInt();
  }
  return v;
}
