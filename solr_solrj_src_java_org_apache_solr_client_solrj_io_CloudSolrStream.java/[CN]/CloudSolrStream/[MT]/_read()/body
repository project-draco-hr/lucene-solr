{
  TupleWrapper tw=tuples.pollFirst();
  if (tw != null) {
    Tuple t=tw.getTuple();
    if (tw.next()) {
      tuples.add(tw);
    }
    return t;
  }
 else {
    Map m=new HashMap();
    m.put("EOF",true);
    return new Tuple(m);
  }
}
