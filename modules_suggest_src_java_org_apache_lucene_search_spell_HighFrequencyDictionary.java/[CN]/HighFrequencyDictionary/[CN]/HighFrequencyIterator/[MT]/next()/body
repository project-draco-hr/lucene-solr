{
  if (!hasNextCalled && !hasNext()) {
    return null;
  }
  hasNextCalled=false;
  if (actualTerm == null) {
    return null;
  }
 else {
    UnicodeUtil.UTF8toUTF16(actualTerm,spare);
    return spare.toString();
  }
}
