{
  if (termsEnum != null) {
    BytesRef next;
    while ((next=termsEnum.next()) != null) {
      if (isFrequent(termsEnum.docFreq())) {
        spare.copyBytes(next);
        return spare;
      }
    }
  }
  return null;
}
