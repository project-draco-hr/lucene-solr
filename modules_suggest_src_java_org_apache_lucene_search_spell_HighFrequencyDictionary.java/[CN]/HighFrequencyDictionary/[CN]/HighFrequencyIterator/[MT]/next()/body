{
  if (termsEnum != null) {
    BytesRef next=termsEnum.next();
    if (next != null && isFrequent(termsEnum.docFreq())) {
      spare.copyBytes(next);
      return spare;
    }
  }
  return null;
}
