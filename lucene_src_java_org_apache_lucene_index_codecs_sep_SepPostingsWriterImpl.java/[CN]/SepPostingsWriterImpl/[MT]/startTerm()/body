{
  docIndex.mark();
  if (!omitTF) {
    freqIndex.mark();
    posIndex.mark();
    payloadStart=payloadOut.getFilePointer();
    lastPayloadLength=-1;
  }
  firstDoc=true;
  skipListWriter.resetSkip(docIndex,freqIndex,posIndex);
}
