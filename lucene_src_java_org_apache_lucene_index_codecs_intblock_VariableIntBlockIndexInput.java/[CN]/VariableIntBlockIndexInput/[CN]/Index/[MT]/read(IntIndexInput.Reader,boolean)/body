{
  if (absolute) {
    fp=indexIn.readVLong();
    upto=indexIn.next() & 0xFF;
  }
 else {
    final long delta=indexIn.readVLong();
    if (delta == 0) {
      upto=indexIn.next() & 0xFF;
    }
 else {
      fp+=delta;
      upto=indexIn.next() & 0xFF;
    }
  }
}
