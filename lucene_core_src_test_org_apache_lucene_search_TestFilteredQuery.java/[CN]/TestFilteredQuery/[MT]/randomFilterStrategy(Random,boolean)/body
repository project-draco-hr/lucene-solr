{
  if (useRandomAccess) {
    return new FilteredQuery.RandomAccessFilterStrategy(){
      @Override protected boolean useRandomAccess(      Bits bits,      long filterCost){
        return true;
      }
    }
;
  }
  return TestUtil.randomFilterStrategy(random);
}
