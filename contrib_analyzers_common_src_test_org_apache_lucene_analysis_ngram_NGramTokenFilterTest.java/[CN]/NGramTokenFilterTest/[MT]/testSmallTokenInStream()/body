{
  input=new WhitespaceTokenizer(new StringReader("abc de fgh"));
  NGramTokenFilter filter=new NGramTokenFilter(input,3,3);
  String[] exp=new String[]{"(abc,0,3)","(fgh,0,3)"};
  checkStream(filter,exp);
}
