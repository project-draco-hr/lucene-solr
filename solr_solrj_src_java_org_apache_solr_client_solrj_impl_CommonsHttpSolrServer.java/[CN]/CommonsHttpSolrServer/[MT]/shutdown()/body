{
  if (shutdownHttpClient && _httpClient != null && _httpClient.getHttpConnectionManager() instanceof MultiThreadedHttpConnectionManager) {
    ((MultiThreadedHttpConnectionManager)_httpClient.getHttpConnectionManager()).shutdown();
  }
}
