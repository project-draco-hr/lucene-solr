{
  initRandom();
  for (int i=0; i <= 32; ++i) {
    double alpha=gen.nextDouble();
    for (int j=0; j <= 32; ++j) {
      tryDistribution(ForPostingsFormat.DEFAULT_BLOCK_SIZE,alpha,MASK[i],MASK[j]);
    }
  }
}
