{
  generateIdentificationProperties();
  setOutputFormat();
  this.extensionProfile=GDataServerRegistry.getRegistry().getExtensionProfile(this.feedId);
  if (this.extensionProfile == null)   throw new GDataRequestException("feed is not registered or extension profile could not be created");
}
