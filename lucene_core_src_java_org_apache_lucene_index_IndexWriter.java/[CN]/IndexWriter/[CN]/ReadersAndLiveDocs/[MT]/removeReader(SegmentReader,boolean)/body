{
  if (sr == reader) {
    reader.decRef();
    reader=null;
  }
  if (sr == mergeReader) {
    mergeReader.decRef();
    mergeReader=null;
    if (drop && reader != null) {
      reader.decRef();
      reader=null;
    }
  }
  return reader != null || mergeReader != null;
}
