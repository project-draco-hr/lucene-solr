{
  IndexInput input=d.openInput(name);
  try {
    size=input.readInt();
    count=input.readInt();
    bits=new byte[(size >> 3) + 1];
    input.readBytes(bits,0,bits.length);
  }
  finally {
    input.close();
  }
}
