{
  Directory dir=newDirectory();
  Directory cr=createLargeCFS(dir);
  try {
    cr.openInput("bogus",newIOContext(random()));
    fail("File not found");
  }
 catch (  IOException e) {
    ;
  }
  cr.close();
  dir.close();
}
