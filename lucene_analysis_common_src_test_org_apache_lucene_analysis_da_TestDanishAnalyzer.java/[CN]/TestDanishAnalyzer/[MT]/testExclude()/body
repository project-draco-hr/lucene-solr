{
  CharArraySet exclusionSet=new CharArraySet(TEST_VERSION_CURRENT,asSet("unders??gelse"),false);
  Analyzer a=new DanishAnalyzer(TEST_VERSION_CURRENT,DanishAnalyzer.getDefaultStopSet(),exclusionSet);
  checkOneTermReuse(a,"unders??gelse","unders??gelse");
  checkOneTermReuse(a,"unders??g","unders??g");
}
