{
  IndexReader reader;
  reader=IndexReader.open(dir);
  assertTrue(reader instanceof MultiSegmentReader);
  assertTrue(dir != null);
  assertTrue(sis != null);
  assertTrue(reader != null);
  return reader;
}
