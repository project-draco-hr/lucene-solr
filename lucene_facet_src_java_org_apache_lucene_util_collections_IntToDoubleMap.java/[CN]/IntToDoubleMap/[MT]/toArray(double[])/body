{
  int j=0;
  if (a.length < this.size()) {
    a=new double[this.size()];
  }
  for (DoubleIterator iterator=iterator(); iterator.hasNext(); ++j) {
    a[j]=iterator.next();
  }
  if (j < a.length) {
    a[j]=Double.NaN;
  }
  return a;
}
