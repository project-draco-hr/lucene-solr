{
  return new Statement(){
    @Override public void evaluate() throws Throwable {
      failures=false;
      try {
        s.evaluate();
      }
 catch (      Throwable t) {
        if (!isAssumption(t)) {
          markFailed();
        }
        throw t;
      }
    }
  }
;
}
