{
  TermQuery query=new TermQuery(new Term(FIELD,"xx"));
  query.setBoost(0);
  Query filtered=new BooleanQuery.Builder().add(query,Occur.MUST).add(matchTheseItems(new int[]{1,3}),Occur.FILTER).build();
  bqtest(filtered,new int[]{3});
}
