{
  if (computeMin) {
    if (null != min && (this.min == null || this.min.after(min))) {
      this.min=min;
    }
  }
  if (computeMax) {
    if (null != max && (this.max == null || this.max.before(max))) {
      this.max=max;
    }
  }
}
