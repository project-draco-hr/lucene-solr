{
  int nextPos=string.indexOf(delim,currPos);
  if (nextPos == -1) {
    nextPos=maxPos + 1;
  }
  String sub;
  if (nextPos > currPos) {
    sub=string.substring(currPos,nextPos);
  }
 else {
    sub="";
  }
  currPos=nextPos + 1;
  return sub;
}
