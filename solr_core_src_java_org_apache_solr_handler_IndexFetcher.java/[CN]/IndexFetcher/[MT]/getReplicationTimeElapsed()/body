{
  long timeElapsed=0;
  if (getReplicationStartTime() > 0)   timeElapsed=TimeUnit.SECONDS.convert(System.currentTimeMillis() - getReplicationStartTime(),TimeUnit.MILLISECONDS);
  return timeElapsed;
}
