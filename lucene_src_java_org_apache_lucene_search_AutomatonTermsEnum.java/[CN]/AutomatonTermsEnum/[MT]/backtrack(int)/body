{
  while (position > 0) {
    char nextChar=utf16.result[position - 1];
    if (nextChar != '\uFFFF') {
      nextChar++;
      utf16.result[position - 1]=nextChar;
      utf16.setLength(position);
      return true;
    }
    position--;
  }
  return false;
}
