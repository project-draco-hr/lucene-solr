{
  if (entity != null) {
    try {
      readFully(entity.getContent());
    }
 catch (    UnsupportedOperationException e) {
    }
catch (    IOException e) {
    }
 finally {
      EntityUtils.consumeQuietly(entity);
    }
  }
}
