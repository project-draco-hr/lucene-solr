{
  encoder.reInit(baos);
  for (  int value : data) {
    encoder.encode(value);
  }
  encoder.close();
  baos.reset();
  encoder.reInit(baos);
  for (  int value : data) {
    encoder.encode(value);
  }
  encoder.close();
}
