{
  int offset=0;
  if (random().nextBoolean()) {
    bytes.grow(bytes.length + 1);
    bytes.offset=1;
    System.arraycopy(bytes.bytes,0,bytes.bytes,1,bytes.length);
    offset=1;
  }
  decoder.decode(bytes,values);
  assertEquals(offset,bytes.offset);
}
