{
  if (file.exists()) {
    try {
      _TestUtil.rmDir(file);
    }
 catch (    IOException e) {
    }
    if (file.exists()) {
      throw new IOException("Could not remove: " + file.getAbsolutePath());
    }
  }
}
