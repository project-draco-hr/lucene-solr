{
  if (distance < 0)   throw new InvalidShapeException("distance must be >= 0; got " + distance);
  if (isGeo())   return new GeoCircleImpl(point,Math.min(distance,maxCircleDistance),this);
 else   return new CircleImpl(point,distance,this);
}
