{
  Thr[] tarr=new Thr[nThreads];
  for (int i=0; i < nThreads; i++) {
    tarr[i]=new Thr(iter,new Random(random.nextLong()));
    tarr[i].start();
  }
  for (int i=0; i < nThreads; i++) {
    tarr[i].join();
  }
  if (failure != null) {
    fail(failure);
  }
}
