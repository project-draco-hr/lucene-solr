{
  dir1=newDirectory();
  buildDir(dir1,15,5,2000);
  int num=100 * RANDOM_MULTIPLIER;
  for (int i=0; i < num; i++) {
    ir1=IndexReader.open(dir1,false);
    doTest(10,10);
    ir1.close();
  }
  dir1.close();
}
