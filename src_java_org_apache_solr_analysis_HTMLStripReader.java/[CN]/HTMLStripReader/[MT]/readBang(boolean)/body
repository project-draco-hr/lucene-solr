{
  int ret=readComment(inScript);
  if (ret == MATCH)   return MATCH;
  int ch=next();
  if (ch == '>')   return MATCH;
  while (true) {
    ch=next();
    if (ch == '>') {
      return MATCH;
    }
 else     if (ch < 0) {
      return MISMATCH;
    }
  }
}
