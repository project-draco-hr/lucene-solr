{
  StringBuffer sb=new StringBuffer();
  int ch, len=s.length();
  for (int i=0; i < len; i++) {
    ch=s.charAt(i);
    if (ch == '"')     sb.append("&quot;");
 else     sb.append((char)ch);
  }
  return sb.toString();
}
