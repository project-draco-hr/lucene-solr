{
  if (keys.hasNext()) {
    currentField=(String)keys.next();
    currentReader=(IndexReader)fieldToReader.get(currentField);
  }
 else {
    currentField=null;
    currentReader=null;
  }
  return currentField;
}
