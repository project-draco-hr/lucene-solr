{
  if (map1 == null)   map1=new HashMap<String,Slice>();
  if (map2 != null) {
    Set<Entry<String,Slice>> entrySet=map2.entrySet();
    for (    Entry<String,Slice> entry : entrySet) {
      map1.put(collection + "_" + entry.getKey(),entry.getValue());
    }
  }
}
