{
  super.setUp();
  queue.clear();
  reset(workQueueMock);
  reset(workQueueMock);
  reset(shardHandlerMock);
  reset(zkStateReaderMock);
  reset(clusterStateMock);
  reset(solrZkClientMock);
  underTest=new OverseerCollectionProcessorToBeTested(zkStateReaderMock,"1234",shardHandlerMock,ADMIN_PATH,workQueueMock);
  zkMap.clear();
  collectionsSet.clear();
}
