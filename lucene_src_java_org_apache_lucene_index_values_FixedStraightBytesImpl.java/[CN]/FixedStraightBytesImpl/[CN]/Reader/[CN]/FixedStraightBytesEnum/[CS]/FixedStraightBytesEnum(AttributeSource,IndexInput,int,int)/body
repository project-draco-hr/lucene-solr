{
  super(source,Values.BYTES_FIXED_STRAIGHT);
  this.datIn=datIn;
  this.size=size;
  this.maxDoc=maxDoc;
  ref=attr.bytes();
  ref.grow(size);
  ref.length=size;
  ref.offset=0;
  fp=datIn.getFilePointer();
}
