{
  byte[] data;
  try {
    data=super.getData(path,watcher,stat,retryOnConnLoss);
  }
 catch (  NoNodeException e) {
    if (isSlowpoke()) {
      Thread.sleep(500);
    }
    throw e;
  }
  return data;
}
