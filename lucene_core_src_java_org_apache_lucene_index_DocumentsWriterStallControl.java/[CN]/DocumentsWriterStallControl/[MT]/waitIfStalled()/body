{
  if (stalled) {
synchronized (this) {
      if (stalled) {
        try {
          incWaiters();
          wait();
          decrWaiters();
        }
 catch (        InterruptedException e) {
          throw new ThreadInterruptedException(e);
        }
      }
    }
  }
}
