{
  if (stalled) {
synchronized (this) {
      if (stalled) {
        try {
          incWaiters();
          wait(1000);
          decrWaiters();
        }
 catch (        InterruptedException e) {
          throw new ThreadInterruptedException(e);
        }
      }
    }
  }
}
