{
  DocCollection c=collectionStates.get(collection);
  if (c != null)   return c;
  if (!stateReader.getAllCollections().contains(collection))   return null;
  return stateReader.getExternCollection(collection,true);
}
