{
  if (routerSpec == null)   return new PlainIdRouter();
  if (DocRouter.DEFAULT_NAME.equals(routerSpec)) {
    return DocRouter.DEFAULT;
  }
  throw new SolrException(ErrorCode.SERVER_ERROR,"Unknown document router '" + routerSpec + "'");
}
