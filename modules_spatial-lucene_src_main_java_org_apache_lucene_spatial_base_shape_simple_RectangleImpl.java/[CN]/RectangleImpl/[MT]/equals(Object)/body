{
  if (obj == null) {
    return false;
  }
  if (obj == this) {
    return true;
  }
  if (obj.getClass() != getClass()) {
    return false;
  }
  RectangleImpl rhs=(RectangleImpl)obj;
  return new EqualsBuilder().append(minX,rhs.minX).append(minY,rhs.minY).append(maxX,rhs.maxX).append(maxY,rhs.maxY).isEquals();
}
