{
  if (Codec.getDefault().getName().equals("Lucene3x")) {
    assertHits(new FunctionQuery(new SumTotalTermFreqValueSource("text")),new float[]{-1f,-1f});
  }
 else {
    assertHits(new FunctionQuery(new SumTotalTermFreqValueSource("text")),new float[]{8f,8f});
  }
}
