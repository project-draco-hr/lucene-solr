{
  triggered=false;
  for (int towait=timeout; towait > 0; towait-=250) {
    try {
      if (triggered) {
        Thread.sleep(500);
        break;
      }
      Thread.sleep(250);
    }
 catch (    InterruptedException e) {
    }
  }
  return triggered;
}
