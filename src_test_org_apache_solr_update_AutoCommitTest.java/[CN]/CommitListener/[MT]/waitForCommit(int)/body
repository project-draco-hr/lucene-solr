{
  triggered=false;
  for (int towait=timeout; towait > 0; towait-=250) {
    if (triggered)     break;
    try {
      Thread.sleep(250);
    }
 catch (    InterruptedException e) {
    }
  }
  return triggered;
}
