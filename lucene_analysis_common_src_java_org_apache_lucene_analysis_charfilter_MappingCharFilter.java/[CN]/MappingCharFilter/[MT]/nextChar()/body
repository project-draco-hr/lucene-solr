{
  if (buffer != null && !buffer.isEmpty()) {
    nextCharCounter++;
    return buffer.removeFirst().charValue();
  }
  int nextChar=input.read();
  if (nextChar != -1) {
    nextCharCounter++;
  }
  return nextChar;
}
