{
  assertQ("No segments mentioned in result",req("qt","/admin/segments"),DOC_COUNT + "=sum(//lst[@name='segments']/lst[*]/int[@name='size'])",DEL_COUNT + "=sum(//lst[@name='segments']/lst[*]/int[@name='delCount'])");
}
