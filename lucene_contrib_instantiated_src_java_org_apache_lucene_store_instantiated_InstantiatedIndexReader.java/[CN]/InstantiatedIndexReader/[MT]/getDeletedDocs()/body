{
  return new Bits(){
    public boolean get(    int n){
      return (index.getDeletedDocuments() != null && index.getDeletedDocuments().get(n)) || (uncommittedDeletedDocuments != null && uncommittedDeletedDocuments.get(n));
    }
    public int length(){
      return maxDoc();
    }
  }
;
}
