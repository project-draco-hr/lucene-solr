{
  for (  FieldInfo fieldInfo : fieldInfos) {
    if (fieldInfo.normsPresent()) {
      String id=docValuesId(segmentName,fieldInfo.number);
      files.add(IndexFileNames.segmentFileName(id,"",segmentSuffix));
    }
  }
}
