{
  Set<String> files=new HashSet<String>();
  filesInternal(state.fieldInfos.hasNorms(),state.segmentName,files,segmentSuffix);
  IOUtils.deleteFilesIgnoringExceptions(state.directory,SegmentInfo.findMatchingFiles(state.directory,files).toArray(new String[0]));
}
