{
  final int charLimit=s.length();
  scratch.grow(charLimit);
  int idx=0;
  while (idx < charLimit) {
    scratch.ints[idx]=(int)s.charAt(idx);
    idx++;
  }
  scratch.offset=0;
  scratch.length=idx;
  return scratch;
}
