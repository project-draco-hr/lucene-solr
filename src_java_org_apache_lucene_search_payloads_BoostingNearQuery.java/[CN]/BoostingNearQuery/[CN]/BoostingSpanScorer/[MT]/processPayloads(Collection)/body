{
  for (Iterator iterator=payLoads.iterator(); iterator.hasNext(); ) {
    byte[] thePayload=(byte[])iterator.next();
    payloadScore=function.currentScore(doc,fieldName,payloadsSeen,payloadScore,similarity.scorePayload(doc,fieldName,thePayload,0,thePayload.length));
    ++payloadsSeen;
  }
}
