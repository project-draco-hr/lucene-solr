{
  if (doc > index.docID()) {
    index.advance(doc);
  }
  int ord;
  if (doc == index.docID()) {
    ord=index.ordValue();
  }
 else {
    ord=-1;
  }
  int slot=ordSet.find(ord);
  if (slot >= 0) {
    return groupDocs[slot];
  }
  return null;
}
