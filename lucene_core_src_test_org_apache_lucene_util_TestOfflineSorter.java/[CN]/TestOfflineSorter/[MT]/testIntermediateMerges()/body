{
  try (Directory dir=newDirectoryNoVirusScanner()){
    SortInfo info=checkSort(dir,new OfflineSorter(dir,"foo",OfflineSorter.DEFAULT_COMPARATOR,BufferSize.megabytes(1),2),generateRandom((int)OfflineSorter.MB * 20));
    assertTrue(info.mergeRounds > 10);
  }
 }
