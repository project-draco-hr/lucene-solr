{
  if (firstTime) {
    init();
  }
 else   if (more) {
    more=last().next();
  }
  while (more && first().doc() < last().doc()) {
    more=first().skipTo(last().doc());
    scorers.addLast(scorers.removeFirst());
  }
  return more;
}
