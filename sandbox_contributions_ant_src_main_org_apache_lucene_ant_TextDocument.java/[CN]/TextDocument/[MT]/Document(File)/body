{
  TextDocument textDoc=new TextDocument(f);
  Document doc=new Document();
  doc.add(Field.Text("contents",textDoc.getContents()));
  doc.add(Field.UnIndexed("rawcontents",textDoc.getContents()));
  return doc;
}
