{
  Tokenizer tokenizer=new WhitespaceTokenizer(Version.LUCENE_CURRENT,new StringReader(input));
  TokenFilter tf=new HindiNormalizationFilter(tokenizer);
  assertTokenStreamContents(tf,new String[]{output});
}
