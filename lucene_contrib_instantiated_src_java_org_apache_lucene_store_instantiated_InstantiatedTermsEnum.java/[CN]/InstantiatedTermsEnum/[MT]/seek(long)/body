{
  upto=start + (int)ord;
  if (upto >= terms.length) {
    return SeekStatus.END;
  }
  if (terms[upto].field().equals(field)) {
    return SeekStatus.FOUND;
  }
 else {
    return SeekStatus.END;
  }
}
