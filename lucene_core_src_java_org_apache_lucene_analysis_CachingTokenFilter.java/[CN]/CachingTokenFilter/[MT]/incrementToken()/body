{
  if (cache == null) {
    cache=new LinkedList<>();
    fillCache();
    iterator=cache.iterator();
  }
  if (!iterator.hasNext()) {
    return false;
  }
  restoreState(iterator.next());
  return true;
}
