{
  if (cache == null) {
    cache=new ArrayList<>(64);
    fillCache();
    iterator=cache.iterator();
  }
  if (!iterator.hasNext()) {
    return false;
  }
  restoreState(iterator.next());
  return true;
}
