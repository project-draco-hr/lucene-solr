{
  if (consumer == null) {
    consumer=normsFormat.docsConsumer(perDocWriteState);
  }
  DocValuesConsumer addValuesField=consumer.addValuesField(Type.BYTES_FIXED_STRAIGHT,fieldInfo);
  return addValuesField;
}
