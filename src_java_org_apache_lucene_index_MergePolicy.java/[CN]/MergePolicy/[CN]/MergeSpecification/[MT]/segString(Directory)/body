{
  StringBuffer b=new StringBuffer();
  b.append("MergeSpec:\n");
  final int count=merges.size();
  for (int i=0; i < count; i++)   b.append("  " + (1 + i) + ": "+ ((OneMerge)merges.get(i)).segString(dir));
  return b.toString();
}
