{
  Token t=new Token(prototype.startOffset() + offset,prototype.startOffset() + offset + length,prototype.type());
  t.setTermBuffer(prototype.termBuffer(),offset,length);
  t.setPositionIncrement(0);
  return t;
}
