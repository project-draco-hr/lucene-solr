{
  if (ref.decrementAndGet() == 0) {
    IOUtils.close(fields,perDocProducer,termVectorsReaderOrig,fieldsReaderOrig,cfsReader,storeCFSReader,norms);
    for (    CoreClosedListener listener : coreClosedListeners) {
      listener.onClose(owner);
    }
  }
}
