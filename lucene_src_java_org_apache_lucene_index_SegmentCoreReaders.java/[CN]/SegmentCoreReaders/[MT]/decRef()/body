{
  if (ref.decrementAndGet() == 0) {
    IOUtils.close(fields,perDocProducer,termVectorsReaderOrig,fieldsReaderOrig,cfsReader,storeCFSReader);
    if (owner != null) {
      owner.notifyReaderFinishedListeners();
    }
  }
}
