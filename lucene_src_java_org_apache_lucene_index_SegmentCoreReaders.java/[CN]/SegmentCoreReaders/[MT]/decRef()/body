{
  if (ref.decrementAndGet() == 0) {
    IOUtils.closeSafely(false,fields,perDocProducer,termVectorsReaderOrig,fieldsReaderOrig,cfsReader,storeCFSReader);
    if (owner != null) {
      owner.notifyReaderFinishedListeners();
    }
  }
}
