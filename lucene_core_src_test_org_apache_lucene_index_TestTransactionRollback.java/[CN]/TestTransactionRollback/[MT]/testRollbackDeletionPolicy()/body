{
  for (int i=0; i < 2; i++) {
    new IndexWriter(dir,newIndexWriterConfig(new MockAnalyzer(random())).setIndexDeletionPolicy(new DeleteLastCommitPolicy())).shutdown();
    IndexReader r=DirectoryReader.open(dir);
    assertEquals(100,r.numDocs());
    r.close();
  }
}
