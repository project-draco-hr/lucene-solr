{
  IndexCommit ic=super.snapshot();
  boolean success=false;
  try {
    persist();
    success=true;
  }
  finally {
    if (!success) {
      try {
        super.release(ic);
      }
 catch (      Exception e) {
      }
    }
  }
  return ic;
}
