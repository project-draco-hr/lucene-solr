{
  if (leafOrd >= leaves.length) {
    return false;
  }
  if (current == null) {
    current=query.getSpans(leaves[leafOrd]);
  }
  while (true) {
    if (current.next()) {
      return true;
    }
    if (++leafOrd < leaves.length) {
      current=query.getSpans(leaves[leafOrd]);
    }
 else {
      current=null;
      break;
    }
  }
  return false;
}
