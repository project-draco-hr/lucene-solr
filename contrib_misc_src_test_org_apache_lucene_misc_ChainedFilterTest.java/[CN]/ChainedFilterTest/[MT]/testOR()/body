{
  for (int mode=0; mode < 2; mode++) {
    boolean old=(mode == 0);
    ChainedFilter chain=getChainedFilter(new Filter[]{sueFilter,bobFilter},null,old);
    int numHits=searcher.search(query,chain,1000).totalHits;
    assertEquals("OR matches all",MAX,numHits);
  }
}
