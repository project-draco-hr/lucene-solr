{
  DocValuesFactory<NumericDocValues> docValuesFactory=numeric.get(field.name);
  if (docValuesFactory == null) {
    numeric.put(field.name,docValuesFactory=new Lucene41NumericDocValues.Factory(this.cfs,this.info,field,context));
  }
  return docValuesFactory.getDirect();
}
