{
  final int n=comparators.length;
  final Comparable[] fields=new Comparable[n];
  for (int i=0; i < n; ++i)   fields[i]=comparators[i].value(entry.slot);
  return new FieldDoc(entry.docID,entry.score,fields);
}
