{
  if (isDouble) {
    double dmax=metricValues.get(MAX);
    if (dmax > doubleMax) {
      doubleMax=dmax;
    }
  }
 else {
    double dmax=metricValues.get(MAX);
    long lmax=(long)dmax;
    if (lmax > longMax) {
      longMax=lmax;
    }
  }
}
