{
  TokenStream stream=whitespaceMockTokenizer("T??rkiye'de 2003'te Van G??l??'n?? g??rd??m");
  stream=new TurkishLowerCaseFilter(stream);
  stream=new ApostropheFilter(stream);
  assertTokenStreamContents(stream,new String[]{"t??rkiye","2003","van","g??l??","g??rd??m"});
}
