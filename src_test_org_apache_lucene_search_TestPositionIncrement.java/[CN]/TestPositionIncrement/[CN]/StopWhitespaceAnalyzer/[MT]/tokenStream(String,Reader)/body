{
  TokenStream ts=a.tokenStream(fieldName,reader);
  return new StopFilter(enablePositionIncrements ? Version.LUCENE_CURRENT : Version.LUCENE_24,ts,new CharArraySet(Version.LUCENE_CURRENT,Collections.singleton("stop"),true));
}
