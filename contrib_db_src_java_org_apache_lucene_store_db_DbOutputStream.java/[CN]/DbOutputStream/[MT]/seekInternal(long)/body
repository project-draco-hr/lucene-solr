{
  if (pos > length)   throw new IOException("seeking past end of file");
  if ((pos >>> BLOCK_SHIFT) == (position >>> BLOCK_SHIFT))   position=pos;
 else {
    block.put(blocks,txn,flags);
    block.seek(pos);
    block.get(blocks,txn,flags);
    position=pos;
  }
}
