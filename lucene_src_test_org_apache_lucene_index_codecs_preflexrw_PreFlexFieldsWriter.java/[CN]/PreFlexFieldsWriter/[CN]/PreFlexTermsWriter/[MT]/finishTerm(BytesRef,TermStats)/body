{
  if (stats.docFreq > 0) {
    long skipPointer=skipListWriter.writeSkip(freqOut);
    termInfo.docFreq=stats.docFreq;
    termInfo.skipOffset=(int)(skipPointer - termInfo.freqPointer);
    termsOut.add(fieldInfo.number,text,termInfo);
  }
}
