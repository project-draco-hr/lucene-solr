{
  datOut.writeByte(FIXED);
  datOut.writeInt(docCount);
  for (int i=0; i < docToValue.length; i++) {
    datOut.writeLong(docToValue[i]);
  }
  for (int i=docToValue.length; i < docCount; i++) {
    datOut.writeLong(0);
  }
}
