{
  List tas=new ArrayList();
  try {
    boolean encore=true;
    Token token;
    while (encore) {
      token=filter.next();
      encore=token != null;
      if (token != null)       tas.add(token.termText());
    }
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
  return tas;
}
