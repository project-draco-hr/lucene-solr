{
  if (size > 1) {
    if (e == null) {
      e=header;
    }
    Entry next=e.next;
    if (next == header) {
      next=next.next;
    }
    return next;
  }
 else   if (size == 1) {
    return header.next;
  }
 else {
    return null;
  }
}
