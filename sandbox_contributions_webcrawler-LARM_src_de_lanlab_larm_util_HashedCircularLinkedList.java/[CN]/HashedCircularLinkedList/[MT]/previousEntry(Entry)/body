{
  if (size > 1) {
    if (e == null) {
      e=header;
    }
    Entry previous=e.previous;
    if (previous == header) {
      previous=previous.previous;
    }
    return previous;
  }
 else   if (size == 1) {
    return header.previous;
  }
 else {
    return null;
  }
}
