{
  Entry e=(Entry)keys.get(o);
  if (e != null) {
    if (e == current) {
      if (size > 1) {
        current=previousEntry(current);
      }
 else {
        current=null;
      }
    }
    this.removeEntryFromList(e);
    keys.remove(o);
    size--;
    return true;
  }
 else {
    return false;
  }
}
