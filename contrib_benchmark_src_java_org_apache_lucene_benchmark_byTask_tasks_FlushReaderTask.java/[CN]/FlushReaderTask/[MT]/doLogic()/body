{
  IndexReader reader=getRunData().getIndexReader();
  if (userData != null) {
    Map map=new HashMap();
    map.put(OpenReaderTask.USER_DATA,userData);
    reader.flush(map);
  }
 else {
    reader.flush();
  }
  return 1;
}
