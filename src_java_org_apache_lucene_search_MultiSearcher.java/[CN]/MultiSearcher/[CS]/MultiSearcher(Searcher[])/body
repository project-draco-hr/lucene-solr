{
  this.searchers=searchers;
  starts=new int[searchers.length + 1];
  for (int i=0; i < searchers.length; i++) {
    starts[i]=maxDoc;
    maxDoc+=searchers[i].maxDoc();
  }
  starts[searchers.length]=maxDoc;
}
