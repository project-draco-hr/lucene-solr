{
  boolean b=false;
  if (allow_mutation)   b=Automaton.setAllowMutate(true);
  Automaton a=toAutomaton(automata,automaton_provider);
  if (allow_mutation)   Automaton.setAllowMutate(b);
  return a;
}
