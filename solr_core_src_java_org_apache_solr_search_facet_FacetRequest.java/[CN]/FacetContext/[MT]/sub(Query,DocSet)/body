{
  FacetContext ctx=new FacetContext();
  ctx.parent=this;
  ctx.base=domain;
  ctx.filter=filter;
  ctx.flags=flags;
  ctx.qcontext=qcontext;
  ctx.req=req;
  ctx.searcher=searcher;
  return ctx;
}
