{
  int seed=1234567;
  int randomState=4 * seed + 1;
  int len=arr.length;
  for (int i=0; i < len - 1; i++) {
    randomState*=0x278DDE6D;
    int r=randomState % (len - i);
    if (r < 0)     r=-r;
    Object tmp=arr[i];
    arr[i]=arr[i + r];
    arr[i + r]=tmp;
  }
}
