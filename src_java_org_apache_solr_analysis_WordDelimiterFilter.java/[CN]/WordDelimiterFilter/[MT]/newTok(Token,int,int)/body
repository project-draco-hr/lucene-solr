{
  int startOff=orig.startOffset();
  int endOff=orig.endOffset();
  String origStr=orig.termText();
  if (origStr.length() == endOff - startOff) {
    endOff=startOff + end;
    startOff+=start;
  }
  return new Token(orig.termText().substring(start,end),startOff,endOff,orig.type());
}
