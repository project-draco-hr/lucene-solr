def lastTempGitSvnRevision(gitCommitMessage):
    parts = gitCommitMessage.split(':')
    lastPart = parts[(-1)].split()[0]
    try:
        return int(lastPart)
    except:
        print 'Warning: svn revision range not found at end of commit message:\n', gitCommitMessage
        return None
