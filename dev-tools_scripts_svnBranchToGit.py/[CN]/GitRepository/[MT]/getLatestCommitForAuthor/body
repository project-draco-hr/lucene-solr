def getLatestCommitForAuthor(self, svnAuthor):
    authorCommit = self.checkOutputAsStr(' '.join((self.gitCmd, 'rev-list', '--all', '-i', ('--author=' + svnAuthor), '|', 'head', '-1')), shell=True)
    authorCommit = authorCommit.rstrip('\n')
    return authorCommit
