{
  Source s=ints.get(id);
  if (s == null) {
    final Reader indexValues=r.getIndexValues(id);
    if (indexValues == null) {
      return null;
    }
    s=indexValues.load();
    ints.put(id,s);
  }
  return s;
}
