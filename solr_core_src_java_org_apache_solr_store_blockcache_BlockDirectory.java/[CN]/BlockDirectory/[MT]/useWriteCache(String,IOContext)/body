{
  if (!blockCacheWriteEnabled) {
    return false;
  }
  if (blockCacheFileTypes != null && !isCachableFile(name)) {
    return false;
  }
switch (context.context) {
case MERGE:
{
      return false;
    }
default :
{
    return true;
  }
}
}
