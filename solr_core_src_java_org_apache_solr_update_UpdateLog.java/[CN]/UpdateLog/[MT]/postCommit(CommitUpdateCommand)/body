{
synchronized (this) {
    if (debug) {
      log.debug("TLOG: postCommit");
    }
    if (prevTlog != null) {
      prevTlog.writeCommit(cmd);
      addOldLog(prevTlog,true);
      prevTlog=null;
    }
  }
}
