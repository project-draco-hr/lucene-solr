{
  if (seekPending) {
    if (pendingFP != lastBlockFP) {
      in.seek(pendingFP);
      lastBlockFP=pendingFP;
      blockReader.readBlock();
    }
    upto=pendingUpto;
    seekPending=false;
  }
}
