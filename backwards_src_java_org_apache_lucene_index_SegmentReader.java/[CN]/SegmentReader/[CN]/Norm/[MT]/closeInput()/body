{
  if (in != null) {
    if (in != singleNormStream) {
      in.close();
    }
 else {
      if (singleNormRef.decRef() == 0) {
        singleNormStream.close();
        singleNormStream=null;
      }
    }
    in=null;
  }
}
