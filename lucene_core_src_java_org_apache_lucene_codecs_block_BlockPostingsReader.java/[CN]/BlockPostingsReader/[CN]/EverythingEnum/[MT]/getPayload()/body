{
  if (DEBUG) {
    System.out.println("    FPR.getPayload payloadLength=" + payloadLength + " payloadByteUpto="+ payloadByteUpto);
  }
  if (payloadLength == 0) {
    return null;
  }
 else {
    return payload;
  }
}
