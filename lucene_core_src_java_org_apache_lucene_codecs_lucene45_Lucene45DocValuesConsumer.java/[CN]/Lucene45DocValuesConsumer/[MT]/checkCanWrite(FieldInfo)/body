{
  if ((field.getDocValuesType() == DocValuesType.NUMERIC || field.getDocValuesType() == DocValuesType.BINARY) && field.getDocValuesGen() != -1) {
  }
 else {
    throw new UnsupportedOperationException("this codec can only be used for reading");
  }
}
