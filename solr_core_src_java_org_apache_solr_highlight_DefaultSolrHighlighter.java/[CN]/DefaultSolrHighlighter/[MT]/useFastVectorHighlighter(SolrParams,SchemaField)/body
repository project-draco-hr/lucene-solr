{
  boolean useFvhParam=params.getFieldBool(schemaField.getName(),HighlightParams.USE_FVH,false);
  if (!useFvhParam)   return false;
  boolean termPosOff=schemaField.storeTermPositions() && schemaField.storeTermOffsets();
  if (!termPosOff) {
    log.warn("Solr will not use FastVectorHighlighter because {} field does not store TermPositions and " + "TermOffsets.",schemaField.getName());
  }
  return termPosOff;
}
