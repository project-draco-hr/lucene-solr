{
  if (heatMin >= cellMin) {
    out[0]=0;
  }
 else {
    out[0]=(int)Math.round((cellMin - heatMin) / heatCellLen);
  }
  if (heatMax <= cellMax) {
    out[1]=heatLen - 1;
  }
 else {
    out[1]=(int)Math.round((cellMax - heatMin) / heatCellLen) - 1;
  }
}
