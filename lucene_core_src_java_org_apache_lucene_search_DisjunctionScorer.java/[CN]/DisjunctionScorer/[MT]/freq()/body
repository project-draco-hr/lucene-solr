{
  if (freq < 0) {
    topScorers=subScorers.topList();
    int freq=1;
    for (ScorerWrapper w=topScorers.next; w != null; w=w.next) {
      freq+=1;
    }
    this.freq=freq;
  }
  return freq;
}
