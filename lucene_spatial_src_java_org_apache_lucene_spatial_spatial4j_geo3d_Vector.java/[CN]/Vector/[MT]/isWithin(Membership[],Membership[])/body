{
  for (  Membership bound : bounds) {
    if (!bound.isWithin(this))     return false;
  }
  for (  Membership bound : moreBounds) {
    if (!bound.isWithin(this))     return false;
  }
  return true;
}
