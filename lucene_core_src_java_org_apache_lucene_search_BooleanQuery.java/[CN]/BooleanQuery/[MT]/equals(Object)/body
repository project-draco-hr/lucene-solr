{
  if (super.equals(o) == false) {
    return false;
  }
  BooleanQuery that=(BooleanQuery)o;
  return this.getMinimumNumberShouldMatch() == that.getMinimumNumberShouldMatch() && this.disableCoord == that.disableCoord && clauses.equals(that.clauses);
}
