{
  ensureDecoded();
  if (shape == iterFilter && cellShapeRel != null)   return cellShapeRel;
  if (shape instanceof LevelledValue)   return relate((LevelledValue)shape);
  if (shape instanceof NRShape)   return relate((NRShape)shape);
  return shape.relate(this).transpose();
}
