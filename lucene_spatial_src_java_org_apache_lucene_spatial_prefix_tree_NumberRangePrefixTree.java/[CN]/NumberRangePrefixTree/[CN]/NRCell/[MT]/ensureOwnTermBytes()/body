{
  NRCell cell0=cellsByLevel[0];
  if (cell0.termBuf == null)   return;
  System.arraycopy(term.bytes,term.offset,cell0.termBuf,0,term.length);
  term.bytes=cell0.termBuf;
  term.offset=0;
  cell0.termBuf=null;
}
