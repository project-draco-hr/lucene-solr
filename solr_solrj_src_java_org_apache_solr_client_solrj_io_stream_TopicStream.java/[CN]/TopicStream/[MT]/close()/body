{
  runCount=0;
  try {
    persistCheckpoints();
  }
  finally {
    if (solrStreams != null) {
      for (      TupleStream solrStream : solrStreams) {
        solrStream.close();
      }
    }
    if (streamContext.getSolrClientCache() == null) {
      cloudSolrClient.close();
    }
  }
}
