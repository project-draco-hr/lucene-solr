{
  super(new AtomicLong(),RamUsageEstimator.NUM_BYTES_INT,ValueType.FIXED_INTS_32);
  final int numDocs=input.readInt();
  values=new int[numDocs];
  adjustSize(numDocs);
  for (int i=0; i < values.length; i++) {
    values[i]=input.readInt();
  }
  maxDocID=numDocs - 1;
}
