{
  return new FixedIntsEnumImpl(attrSource,input,type()){
    @Override protected void fillNext(    LongsRef ref,    IndexInput dataIn) throws IOException {
      ref.ints[ref.offset]=dataIn.readShort();
    }
  }
;
}
