{
  super(Counter.newCounter(),RamUsageEstimator.NUM_BYTES_SHORT,ValueType.FIXED_INTS_16);
  values=new short[numDocs];
  adjustSize(numDocs);
  for (int i=0; i < values.length; i++) {
    values[i]=input.readShort();
  }
  maxDocID=numDocs - 1;
}
