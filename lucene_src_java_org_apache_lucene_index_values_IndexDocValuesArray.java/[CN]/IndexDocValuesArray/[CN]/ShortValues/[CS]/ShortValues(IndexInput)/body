{
  super(new AtomicLong(),RamUsageEstimator.NUM_BYTES_SHORT,ValueType.FIXED_INTS_16);
  final int numDocs=input.readInt();
  values=new short[numDocs];
  adjustSize(numDocs);
  for (int i=0; i < values.length; i++) {
    values[i]=input.readShort();
  }
  maxDocID=numDocs - 1;
}
