{
  return new FloatsEnum(attrSource,input,type(),bytesPerValue,maxDoc){
    @Override protected double toDouble(    BytesRef bytesRef){
      return Float.intBitsToFloat(bytesRef.asInt());
    }
  }
;
}
