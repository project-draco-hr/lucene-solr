{
  return new FixedIntsEnum(attrSource,input,type(),bytesPerValue,maxDoc){
    @Override protected final long toLong(    BytesRef bytesRef){
      return bytesRef.bytes[bytesRef.offset];
    }
  }
;
}
