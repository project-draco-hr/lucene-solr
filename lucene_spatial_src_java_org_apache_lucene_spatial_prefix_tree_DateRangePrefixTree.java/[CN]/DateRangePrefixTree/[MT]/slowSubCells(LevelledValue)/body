{
  int field=FIELD_BY_LEVEL[lv.getLevel() + 1];
  if (field == -1 || field == Calendar.YEAR || field >= Calendar.HOUR_OF_DAY)   return super.getNumSubCells(lv);
  Calendar cal=toCalendarLV(lv);
  return cal.getActualMaximum(field) - cal.getActualMinimum(field) + 1;
}
