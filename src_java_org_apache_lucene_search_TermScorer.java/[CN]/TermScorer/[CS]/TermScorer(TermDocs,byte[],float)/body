{
  termDocs=td;
  norms=n;
  weight=w;
  for (int i=0; i < SCORE_CACHE_SIZE; i++)   scoreCache[i]=Similarity.tf(i) * weight;
  pointerMax=termDocs.read(docs,freqs);
  if (pointerMax != 0)   doc=docs[0];
 else {
    termDocs.close();
    doc=Integer.MAX_VALUE;
  }
}
