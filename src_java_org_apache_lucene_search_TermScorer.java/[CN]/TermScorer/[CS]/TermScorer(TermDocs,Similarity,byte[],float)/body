{
  super(similarity);
  this.termDocs=td;
  this.norms=norms;
  this.weight=weight;
  for (int i=0; i < SCORE_CACHE_SIZE; i++)   scoreCache[i]=getSimilarity().tf(i) * weight;
  pointerMax=termDocs.read(docs,freqs);
  if (pointerMax != 0)   doc=docs[0];
 else {
    termDocs.close();
    doc=Integer.MAX_VALUE;
  }
}
