{
  final int len=text.length();
  if (termBuffer == null || termBuffer.length < 1 + len)   termBuffer=new char[ArrayUtil.getNextSize(1 + len)];
  text.getChars(0,len,termBuffer,0);
  termBuffer[len]=0xffff;
  return addTerm(termBuffer,0);
}
