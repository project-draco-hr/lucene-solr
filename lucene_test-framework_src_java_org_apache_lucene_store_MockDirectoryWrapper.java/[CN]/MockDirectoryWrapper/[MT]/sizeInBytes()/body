{
  if (in instanceof RAMDirectory)   return ((RAMDirectory)in).ramBytesUsed();
 else {
    long size=0;
    for (    String file : in.listAll())     size+=in.fileLength(file);
    return size;
  }
}
