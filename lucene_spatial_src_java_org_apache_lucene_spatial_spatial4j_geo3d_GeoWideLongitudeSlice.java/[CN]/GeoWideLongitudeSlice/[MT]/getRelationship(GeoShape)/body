{
  if (path.intersects(leftPlane) || path.intersects(rightPlane))   return OVERLAPS;
  if (isWithin(path.getInteriorPoint()))   return WITHIN;
  if (path.isWithin(centerPoint))   return CONTAINS;
  return DISJOINT;
}
