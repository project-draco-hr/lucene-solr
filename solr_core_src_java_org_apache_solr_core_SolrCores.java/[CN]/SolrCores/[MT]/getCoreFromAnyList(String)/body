{
  SolrCore core;
synchronized (modifyLock) {
    core=cores.get(name);
    if (core != null) {
      return core;
    }
    if (dynamicDescriptors.size() == 0) {
      return null;
    }
    return transientCores.get(name);
  }
}
