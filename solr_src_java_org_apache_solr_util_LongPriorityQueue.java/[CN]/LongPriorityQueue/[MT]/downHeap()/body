{
  int i=1;
  long node=heap[i];
  int j=i << 1;
  int k=j + 1;
  if (k <= size && heap[k] < heap[j]) {
    j=k;
  }
  while (j <= size && heap[j] < node) {
    heap[i]=heap[j];
    i=j;
    j=i << 1;
    k=j + 1;
    if (k <= size && heap[k] < heap[j]) {
      j=k;
    }
  }
  heap[i]=node;
}
