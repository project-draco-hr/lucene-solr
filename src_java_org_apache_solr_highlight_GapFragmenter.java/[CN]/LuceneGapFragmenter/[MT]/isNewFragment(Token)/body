{
  boolean isNewFrag=token.endOffset() >= fragOffset + getFragmentSize() || token.getPositionIncrement() > INCREMENT_THRESHOLD;
  if (isNewFrag) {
    fragOffset=token.endOffset();
  }
  return isNewFrag;
}
