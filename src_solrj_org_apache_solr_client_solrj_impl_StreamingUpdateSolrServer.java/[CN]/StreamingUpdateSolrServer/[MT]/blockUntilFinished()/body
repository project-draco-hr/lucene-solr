{
  if (lock == null) {
    lock=new ReentrantLock();
  }
  lock.lock();
  Runner runner=runners.peek();
  while (runner != null) {
    runner.lock.lock();
    runner.lock.unlock();
    runner=runners.peek();
  }
  lock.unlock();
  lock=null;
}
