{
  try (InputStream affix=loader.openResource(affixFile);InputStream dictionary=loader.openResource(dictionaryFile)){
    try {
      this.dictionary=new Dictionary(affix,dictionary);
    }
 catch (    ParseException e) {
      throw new RuntimeException(e);
    }
  }
 }
