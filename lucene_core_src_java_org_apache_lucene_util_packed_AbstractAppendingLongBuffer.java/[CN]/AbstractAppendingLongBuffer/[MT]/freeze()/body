{
  if (pendingOff > 0) {
    if (deltas.length == valuesOff) {
      grow(valuesOff + 1);
    }
    packPendingValues();
    deltasBytes+=deltas[valuesOff].ramBytesUsed();
    ++valuesOff;
    pendingOff=0;
  }
  pending=null;
}
