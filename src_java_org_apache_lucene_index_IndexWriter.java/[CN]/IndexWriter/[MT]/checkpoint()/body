{
  if (autoCommit) {
    segmentInfos.write(directory);
    if (infoStream != null)     infoStream.println("checkpoint: wrote segments file \"" + segmentInfos.getCurrentSegmentFileName() + "\"");
  }
 else {
    commitPending=true;
  }
}
