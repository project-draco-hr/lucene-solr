{
  int code=0;
  int len=text.length();
  if (ignoreCase) {
    for (int i=0; i < len; i++) {
      code=code * 31 + Character.toLowerCase(text.charAt(i));
    }
  }
 else {
    for (int i=0; i < len; i++) {
      code=code * 31 + text.charAt(i);
    }
  }
  return code;
}
