{
  StringBuilder sb=new StringBuilder();
  for (DocIterator iter=dl.iterator(); iter.hasNext(); ) {
    sb.append(iter.nextDoc()).append(':').append(iter.score()).append(',');
  }
  if (sb.length() > 0) {
    rb.rsp.addToLog("responseLog",sb.substring(0,sb.length() - 1));
  }
}
