{
  int count=0;
  DocsEnum td=MultiFields.getTermDocsEnum(r,MultiFields.getDeletedDocs(r),t.field(),new BytesRef(t.text()));
  if (td != null) {
    while (td.nextDoc() != DocsEnum.NO_MORE_DOCS) {
      td.docID();
      count++;
    }
  }
  return count;
}
