{
  int count=0;
  DocsEnum td=_TestUtil.docs(random,r,t.field(),new BytesRef(t.text()),MultiFields.getLiveDocs(r),null,false);
  if (td != null) {
    while (td.nextDoc() != DocsEnum.NO_MORE_DOCS) {
      td.docID();
      count++;
    }
  }
  return count;
}
