{
  while (true) {
    if (count == docFreq) {
      return doc=NO_MORE_DOCS;
    }
    count++;
    doc+=docReader.next();
    freq=freqReader.next();
    pendingPosCount+=freq;
    if (skipDocs == null || !skipDocs.get(doc)) {
      break;
    }
  }
  position=0;
  return doc;
}
