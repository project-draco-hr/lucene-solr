{
  while (true) {
    if (count == docFreq) {
      return doc=NO_MORE_DOCS;
    }
    count++;
    doc+=docReader.next();
    if (!omitTF) {
      freq=freqReader.next();
    }
    if (skipDocs == null || !skipDocs.get(doc)) {
      break;
    }
  }
  return doc;
}
