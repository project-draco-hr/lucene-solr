{
  while (true) {
    if (count == docFreq) {
      return doc=NO_MORE_DOCS;
    }
    count++;
    doc+=docReader.next();
    if (!omitTF) {
      freq=freqReader.next();
    }
    if (liveDocs == null || liveDocs.get(doc)) {
      break;
    }
  }
  return doc;
}
