{
  if (nullDoc > -1) {
    this.collapsedSet.fastSet(nullDoc);
  }
  for (int i=0; i < ords.length; i++) {
    int doc=ords[i];
    if (doc > -1) {
      collapsedSet.fastSet(doc);
    }
  }
  return collapsedSet;
}
