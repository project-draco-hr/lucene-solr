{
  final IndexInput input=cloneData();
  boolean success=false;
  try {
    final ValuesEnum valuesEnum=arrayTemplate.getDirectEnum(source,input,maxDoc);
    success=true;
    return valuesEnum;
  }
  finally {
    if (!success) {
      IOUtils.closeWhileHandlingException(input);
    }
  }
}
