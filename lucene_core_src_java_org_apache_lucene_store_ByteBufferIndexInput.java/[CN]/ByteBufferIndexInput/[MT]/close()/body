{
  try {
    if (buffers == null)     return;
    final ByteBuffer[] bufs=buffers;
    unsetBuffers();
    if (isClone)     return;
    guard.invalidateAndUnmap(bufs);
  }
  finally {
    unsetBuffers();
  }
}
