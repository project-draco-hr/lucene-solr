{
  if (version > 0) {
    for (; ; ) {
      long badVersion=rand.nextInt();
      if (badVersion != version && badVersion != 0)       return badVersion;
    }
  }
  for (; ; ) {
    long badVersion=rand.nextInt() & 0x7fffffff;
    if (badVersion != 0)     return badVersion;
  }
}
