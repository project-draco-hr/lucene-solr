{
  if (_wantsAllFields || okFieldNames.contains(name)) {
    return true;
  }
  for (  String s : globs) {
    if (FilenameUtils.wildcardMatch(name,s)) {
      okFieldNames.add(name);
      return true;
    }
  }
  return false;
}
