{
  for (  SegmentEndpoint pathPoint : points) {
    if (pathPoint.intersects(plane,bounds))     return true;
  }
  for (  PathSegment pathSegment : segments) {
    if (pathSegment.intersects(plane,bounds))     return true;
  }
  return false;
}
