{
  for (  SegmentEndpoint pathPoint : points) {
    if (pathPoint.isWithin(point))     return true;
  }
  for (  PathSegment pathSegment : segments) {
    if (pathSegment.isWithin(point))     return true;
  }
  return false;
}
