{
  int version=CodecUtil.checkHeader(input,TempBlockTermsWriter.TERMS_CODEC_NAME,TempBlockTermsWriter.TERMS_VERSION_START,TempBlockTermsWriter.TERMS_VERSION_CURRENT);
  if (version < TempBlockTermsWriter.TERMS_VERSION_APPEND_ONLY) {
    dirOffset=input.readLong();
  }
  return version;
}
