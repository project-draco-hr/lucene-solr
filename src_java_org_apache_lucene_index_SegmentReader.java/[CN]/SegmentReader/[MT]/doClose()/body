{
  termVectorsLocal.close();
  fieldsReaderLocal.close();
  if (deletedDocs != null) {
    deletedDocsRef.decRef();
    deletedDocs=null;
  }
  Iterator it=norms.values().iterator();
  while (it.hasNext()) {
    ((Norm)it.next()).decRef();
  }
  if (core != null) {
    core.decRef();
  }
}
