{
  Norm norm=(Norm)norms.get(field);
  if (norm == null)   return null;
synchronized (norm) {
    if (norm.bytes == null) {
      byte[] bytes=new byte[maxDoc()];
      norms(field,bytes,0);
      norm.bytes=bytes;
      norm.close();
    }
    return norm.bytes;
  }
}
