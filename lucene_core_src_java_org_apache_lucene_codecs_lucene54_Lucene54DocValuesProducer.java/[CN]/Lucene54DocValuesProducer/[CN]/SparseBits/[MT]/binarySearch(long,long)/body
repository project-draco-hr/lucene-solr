{
  while (index + 1 < hiIndex) {
    final long midIndex=(index + hiIndex) >>> 1;
    final long midDocId=docIds.get(midIndex);
    if (midDocId > docId) {
      hiIndex=midIndex;
      nextDocId=midDocId;
    }
 else {
      index=midIndex;
      this.docId=midDocId;
    }
  }
}
