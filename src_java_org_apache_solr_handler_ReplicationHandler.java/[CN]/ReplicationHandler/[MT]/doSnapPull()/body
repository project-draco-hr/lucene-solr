{
  if (!isSlave)   return;
  if (!snapPullLock.tryLock())   return;
  try {
    snapPuller.fetchLatestIndex(core);
  }
 catch (  Exception e) {
    LOG.error("SnapPull failed ",e);
  }
 finally {
    snapPullLock.unlock();
  }
}
