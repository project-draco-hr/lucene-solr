{
  return p.intersects(topPlane,bounds,bottomPlane,eitherBound) || p.intersects(bottomPlane,bounds,topPlane,eitherBound) || p.intersects(leftPlane,bounds,topPlane,bottomPlane)|| p.intersects(rightPlane,bounds,topPlane,bottomPlane);
}
