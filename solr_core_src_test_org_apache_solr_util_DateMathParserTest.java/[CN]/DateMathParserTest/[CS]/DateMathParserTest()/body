{
  super();
  fmt=new SimpleDateFormat("G yyyyy MM ww WW DD dd F E aa HH hh mm ss SSS z Z",Locale.ROOT);
  fmt.setTimeZone(UTC);
  parser=new SimpleDateFormat("yyyy-MM-dd'T'HH:mm:ss.SSS",Locale.ROOT);
  parser.setTimeZone(UTC);
}
