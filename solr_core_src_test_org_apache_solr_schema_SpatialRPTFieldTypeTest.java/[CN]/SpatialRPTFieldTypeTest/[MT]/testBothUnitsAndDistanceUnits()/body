{
  try {
    setupRPTField("degrees","kilometers","true");
    fail("Expected exception for deprecated units parameter.");
  }
 catch (  Exception ex) {
    if (!ex.getMessage().startsWith("units parameter is deprecated"))     throw ex;
  }
}
