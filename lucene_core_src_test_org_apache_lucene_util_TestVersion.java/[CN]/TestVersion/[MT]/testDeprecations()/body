{
  Version values[]=Version.values();
  for (int i=0; i < values.length - 2; i++) {
    assertTrue(values[i].name() + " should be deprecated",Version.class.getField(values[i].name()).isAnnotationPresent(Deprecated.class));
  }
}
