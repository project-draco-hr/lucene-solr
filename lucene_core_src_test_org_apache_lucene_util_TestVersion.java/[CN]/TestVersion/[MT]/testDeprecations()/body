{
  Version values[]=Version.values();
  for (int i=0; i < values.length - 2; i++) {
    assertNotNull(values[i].name() + " should be deprecated",Version.class.getField(values[i].name()).getAnnotation(Deprecated.class));
  }
}
