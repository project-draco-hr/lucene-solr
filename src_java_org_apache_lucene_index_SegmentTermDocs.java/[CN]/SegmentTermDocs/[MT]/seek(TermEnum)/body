{
  TermInfo ti;
  if (enum instanceof SegmentTermEnum && ((SegmentTermEnum)enum).fieldInfos == parent.fieldInfos)   ti=((SegmentTermEnum)enum).termInfo();
 else   ti=parent.tis.get(enum.term());
  seek(ti);
}
