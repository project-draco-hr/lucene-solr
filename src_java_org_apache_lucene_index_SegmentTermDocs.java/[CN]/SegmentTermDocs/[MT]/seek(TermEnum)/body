{
  TermInfo ti;
  if (termEnum instanceof SegmentTermEnum && ((SegmentTermEnum)termEnum).fieldInfos == parent.fieldInfos)   ti=((SegmentTermEnum)termEnum).termInfo();
 else   ti=parent.tis.get(termEnum.term());
  seek(ti);
}
