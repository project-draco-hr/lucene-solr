{
  final long delGen=bufferedDeletesStream.getNextGen();
  if (pendingDeletes.any()) {
    indexWriter.bufferedDeletesStream.push(new FrozenBufferedDeletes(pendingDeletes,delGen));
    pendingDeletes.clear();
  }
}
