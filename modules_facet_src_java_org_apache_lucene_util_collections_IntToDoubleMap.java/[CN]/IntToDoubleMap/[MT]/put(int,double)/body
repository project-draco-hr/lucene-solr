{
  int index=find(key);
  if (index != 0) {
    double old=values[index];
    values[index]=v;
    return old;
  }
  if (size == capacity) {
    grow();
  }
  prvt_put(key,v);
  return Double.NaN;
}
