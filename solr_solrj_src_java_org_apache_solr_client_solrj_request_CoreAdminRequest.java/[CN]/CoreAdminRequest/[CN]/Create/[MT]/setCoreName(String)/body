{
  if (!SolrIdentifierValidator.validateCoreName(coreName)) {
    throw new IllegalArgumentException("Invalid collection: " + coreName + ". Core names must consist entirely of periods, underscores, and alphanumerics");
  }
  this.core=coreName;
}
