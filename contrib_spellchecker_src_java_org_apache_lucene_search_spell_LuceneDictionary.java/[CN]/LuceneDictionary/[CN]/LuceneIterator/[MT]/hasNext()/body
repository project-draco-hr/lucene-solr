{
  has_next_called=true;
  try {
    if (!termEnum.next()) {
      actualTerm=null;
      return false;
    }
    actualTerm=termEnum.term();
    String fieldt=actualTerm.field();
    if (fieldt != field) {
      actualTerm=null;
      return false;
    }
    return true;
  }
 catch (  IOException ex) {
    ex.printStackTrace();
    return false;
  }
}
