{
  Comparable nComp=delegate.getValue();
  if (nComp == null) {
    return null;
  }
  double d=((Number)nComp).doubleValue();
  if (d < 0) {
    return new Double(d * -1);
  }
 else {
    return new Double(d);
  }
}
