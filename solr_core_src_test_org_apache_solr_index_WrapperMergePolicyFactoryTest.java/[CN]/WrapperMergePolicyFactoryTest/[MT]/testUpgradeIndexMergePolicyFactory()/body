{
  final int N=10;
  final Double wrappingNoCFSRatio=random().nextBoolean() ? null : random().nextInt(N + 1) / ((double)N);
  final Double wrappedNoCFSRatio=random().nextBoolean() ? null : random().nextInt(N + 1) / ((double)N);
  implTestUpgradeIndexMergePolicyFactory(wrappingNoCFSRatio,wrappedNoCFSRatio);
}
