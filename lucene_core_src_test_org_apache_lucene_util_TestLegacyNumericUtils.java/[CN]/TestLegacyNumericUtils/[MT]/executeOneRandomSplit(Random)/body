{
  long lower=randomLong(random);
  long len=random.nextInt(16384 * 1024);
  while (lower + len < lower) {
    lower>>=1;
  }
  assertLongRangeSplit(lower,lower + len,random.nextInt(64) + 1,true,null,null);
}
