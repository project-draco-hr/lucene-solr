{
  String relativePath=root.relativize(file).toString();
  String separator=root.getFileSystem().getSeparator();
  if ("\\".equals(separator))   relativePath=relativePath.replaceAll("\\\\","/");
  return zkRoot + "/" + relativePath;
}
