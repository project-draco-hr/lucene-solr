{
  if (v1 == v2) {
    return 0;
  }
 else   if (v1 == null) {
    return -1;
  }
 else   if (v2 == null) {
    return 1;
  }
  long t1=getLong(v1);
  long t2=getLong(v2);
  return (t1 < t2 ? -1 : (t1 == t2 ? 0 : 1));
}
