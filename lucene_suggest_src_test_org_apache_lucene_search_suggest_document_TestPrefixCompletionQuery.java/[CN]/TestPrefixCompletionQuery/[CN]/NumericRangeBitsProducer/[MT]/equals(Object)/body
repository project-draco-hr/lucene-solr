{
  if (obj == null || getClass() != obj.getClass()) {
    return false;
  }
  NumericRangeBitsProducer that=(NumericRangeBitsProducer)obj;
  return field.equals(that.field) && min == that.min && max == that.max;
}
