{
  if (input.incrementToken()) {
    if (!keywordAtt.isKeyword() && termAtt.length() > minLength) {
      StringBuilder sb=stemmer.stem(termAtt);
      if (sb != null)       termAtt.setEmpty().append(sb);
    }
    return true;
  }
 else {
    return false;
  }
}
