{
  if (!input.incrementToken()) {
    return false;
  }
  if (offsetAttrib.startOffset() <= maxStartOffset) {
    return true;
  }
  if (consumeAllTokens) {
    while (input.incrementToken()) {
    }
  }
  return false;
}
