{
  if (count == freq) {
    if (postings == null) {
      return false;
    }
    doc=postings.nextDoc();
    if (doc == DocsAndPositionsEnum.NO_MORE_DOCS) {
      return false;
    }
    freq=postings.freq();
    count=0;
  }
  position=postings.nextPosition();
  count++;
  return true;
}
