{
  if (clauses.size() == 0)   return new SpanOrQuery(getClauses()).getSpans(context);
  if (clauses.size() == 1)   return clauses.get(0).getSpans(context);
  return inOrder ? (Spans)new NearSpansOrdered(this,context,collectPayloads) : (Spans)new NearSpansUnordered(this,context);
}
