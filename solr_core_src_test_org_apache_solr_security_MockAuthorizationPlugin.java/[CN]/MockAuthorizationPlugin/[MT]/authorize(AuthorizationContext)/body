{
  log.info("User request: " + context.getParams().get("uname"));
  if (denyUsers.contains(context.getParams().get("uname")))   return new AuthorizationResponse(403);
 else   return new AuthorizationResponse(200);
}
