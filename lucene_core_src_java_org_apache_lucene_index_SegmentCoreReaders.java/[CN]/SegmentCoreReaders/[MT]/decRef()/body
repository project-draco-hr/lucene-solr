{
  if (ref.decrementAndGet() == 0) {
    IOUtils.close(termVectorsLocal,fieldsReaderLocal,simpleDocValuesLocal,simpleNormsLocal,fields,simpleDVProducer,perDocProducer,termVectorsReaderOrig,fieldsReaderOrig,cfsReader,norms,simpleNormsProducer);
    notifyCoreClosedListeners();
  }
}
