{
  if (ref.decrementAndGet() == 0) {
    IOUtils.close(termVectorsLocal,fieldsReaderLocal,fields,simpleDVProducer,perDocProducer,termVectorsReaderOrig,fieldsReaderOrig,cfsReader,norms,simpleNormsProducer);
    notifyCoreClosedListeners();
  }
}
