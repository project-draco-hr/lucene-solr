{
  if (multiValued) {
    List<String> v=(List<String>)values.get(fieldName);
    if (v == null) {
      v=new ArrayList<>();
      values.put(fieldName,v);
    }
    v.add(value);
  }
 else {
    values.put(fieldName,value);
  }
}
