{
  if (documentCache == null) {
    documentCache=new HashMap<String,Document>();
    for (    Document document : documents)     documentCache.put(document.name,document);
  }
  return documentCache.get(name);
}
