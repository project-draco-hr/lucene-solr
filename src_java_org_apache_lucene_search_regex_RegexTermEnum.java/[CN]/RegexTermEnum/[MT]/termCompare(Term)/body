{
  if (field == term.field()) {
    String searchText=term.text();
    if (searchText.startsWith(pre)) {
      return pattern.matcher(searchText).matches();
    }
  }
  endEnum=true;
  return false;
}
