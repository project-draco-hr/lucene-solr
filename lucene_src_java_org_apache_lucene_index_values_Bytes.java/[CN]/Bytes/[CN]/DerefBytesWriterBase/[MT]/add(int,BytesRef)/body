{
  if (bytes.length == 0) {
    return;
  }
  checkSize(bytes);
  fillDefault(docID);
  int ord=hash.add(bytes);
  if (ord < 0) {
    ord=(-ord) - 1;
  }
  docToEntry[docID]=ord;
  lastDocId=docID;
}
