{
  if (tokenIter != null && tokenIter.hasNext())   return (Token)tokenIter.next();
 else {
    Token nextToken=input.next(reusableSentenceToken);
    if (processNextSentence(nextToken)) {
      return (Token)tokenIter.next();
    }
 else     return null;
  }
}
