{
  Iterator it=logs.iterator();
  IOException ex=null;
  while (it.hasNext()) {
    try {
      SimpleLogger logger=(SimpleLogger)it.next();
      logger.flush();
    }
 catch (    IOException e) {
      ex=e;
    }
  }
  if (ex != null) {
    throw ex;
  }
}
