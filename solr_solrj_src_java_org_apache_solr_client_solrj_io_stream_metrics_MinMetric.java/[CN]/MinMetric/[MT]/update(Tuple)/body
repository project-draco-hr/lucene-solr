{
  Object o=tuple.get(column);
  if (o instanceof Double) {
    double d=(double)o;
    if (d < doubleMin) {
      doubleMin=d;
    }
  }
 else {
    long l=(long)o;
    if (l < longMin) {
      longMin=l;
    }
  }
}
