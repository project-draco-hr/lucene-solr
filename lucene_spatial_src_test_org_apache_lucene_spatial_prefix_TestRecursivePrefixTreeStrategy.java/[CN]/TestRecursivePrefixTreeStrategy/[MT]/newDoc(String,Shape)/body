{
  Document doc=new Document();
  doc.add(new StringField("id",id,Field.Store.YES));
  for (  IndexableField f : strategy.createFields(shape)) {
    doc.add(f);
  }
  if (storeShape)   doc.add(strategy.createStoredField(shape));
  return doc;
}
