{
  if (documentsWriter.infoStream.isEnabled("DWFC")) {
    documentsWriter.infoStream.message("DWFC","waitIfStalled: numFlushesPending: " + flushQueue.size() + " netBytes: "+ netBytes()+ " flushBytes: "+ flushBytes()+ " fullFlush: "+ fullFlush);
  }
  stallControl.waitIfStalled();
}
