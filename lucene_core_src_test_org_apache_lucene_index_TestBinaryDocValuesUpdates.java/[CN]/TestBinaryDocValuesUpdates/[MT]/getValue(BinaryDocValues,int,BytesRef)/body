{
  bdv.get(idx,scratch);
  idx=scratch.offset;
  byte b=scratch.bytes[idx++];
  long value=b & 0x7FL;
  for (int shift=7; (b & 0x80L) != 0; shift+=7) {
    b=scratch.bytes[idx++];
    value|=(b & 0x7FL) << shift;
  }
  return value;
}
