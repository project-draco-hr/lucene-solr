{
  if (jarContent == null)   throw new SolrException(SolrException.ErrorCode.SERVER_ERROR,"jar not available: " + name + "/"+ version);
  return BlobRepository.getFileContent(jarContent.blob,entryName);
}
