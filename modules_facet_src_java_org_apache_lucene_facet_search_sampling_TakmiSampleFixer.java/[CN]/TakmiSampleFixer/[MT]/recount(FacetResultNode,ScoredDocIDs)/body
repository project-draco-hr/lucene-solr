{
  CategoryPath catPath=fresNode.getLabel(taxonomyReader);
  Term drillDownTerm=DrillDown.term(searchParams,catPath);
  Bits liveDocs=MultiFields.getLiveDocs(indexReader);
  int updatedCount=countIntersection(MultiFields.getTermDocsEnum(indexReader,liveDocs,drillDownTerm.field(),drillDownTerm.bytes()),docIds.iterator());
  fresNode.setValue(updatedCount);
}
