{
  if (super.equals(obj) == false) {
    return false;
  }
  ValueSourceFilter other=(ValueSourceFilter)obj;
  return startingFilter.equals(other.startingFilter) && source.equals(other.source) && min == min && max == max;
}
