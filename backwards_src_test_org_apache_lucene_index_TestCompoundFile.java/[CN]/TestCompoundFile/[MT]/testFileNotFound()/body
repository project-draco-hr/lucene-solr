{
  setUp_2();
  CompoundFileReader cr=new CompoundFileReader(dir,"f.comp");
  try {
    IndexInput e1=cr.openInput("bogus");
    fail("File not found");
  }
 catch (  IOException e) {
  }
  cr.close();
}
