{
  super(directory);
  this.readers=readers;
  starts=new int[readers.length + 1];
  for (int i=0; i < readers.length; i++) {
    starts[i]=maxDoc;
    maxDoc+=readers[i].maxDoc();
    if (readers[i].hasDeletions())     hasDeletions=true;
  }
  starts[readers.length]=maxDoc;
}
