{
  byte[] bytes=(byte[])normsCache.get(field);
  if (bytes != null)   System.arraycopy(bytes,0,result,offset,maxDoc());
  for (int i=0; i < readers.length; i++)   readers[i].norms(field,result,offset + starts[i]);
}
