{
  byte[] bytes=(byte[])normsCache.get(field);
  if (bytes != null)   return bytes;
  bytes=new byte[maxDoc()];
  for (int i=0; i < readers.length; i++)   readers[i].norms(field,bytes,starts[i]);
  normsCache.put(field,bytes);
  return bytes;
}
