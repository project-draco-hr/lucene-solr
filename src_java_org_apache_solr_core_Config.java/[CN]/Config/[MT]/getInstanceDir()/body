{
  String str=System.getProperty(instance + ".solr.home");
  if (str == null) {
    str=instance + '/';
  }
 else   if (!(str.endsWith("/") || str.endsWith("\\"))) {
    str+='/';
  }
  return str;
}
