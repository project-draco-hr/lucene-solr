{
  if (!(o instanceof PhraseQuery))   return false;
  PhraseQuery other=(PhraseQuery)o;
  return super.equals(o) && (this.slop == other.slop) && this.terms.equals(other.terms)&& this.positions.equals(other.positions);
}
