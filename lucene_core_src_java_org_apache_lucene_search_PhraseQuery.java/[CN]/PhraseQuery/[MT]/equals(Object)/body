{
  if (super.equals(o) == false) {
    return false;
  }
  PhraseQuery that=(PhraseQuery)o;
  return slop == that.slop && Arrays.equals(terms,that.terms) && Arrays.equals(positions,that.positions);
}
