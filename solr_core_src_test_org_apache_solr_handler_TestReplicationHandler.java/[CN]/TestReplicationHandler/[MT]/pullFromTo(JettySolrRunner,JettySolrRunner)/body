{
  String masterUrl;
  URL url;
  InputStream stream;
  masterUrl=buildUrl(to.getLocalPort()) + "/" + DEFAULT_TEST_CORENAME+ "/replication?wait=true&command=fetchindex&masterUrl="+ buildUrl(from.getLocalPort())+ "/"+ DEFAULT_TEST_CORENAME+ "/replication";
  url=new URL(masterUrl);
  stream=url.openStream();
  stream.close();
}
