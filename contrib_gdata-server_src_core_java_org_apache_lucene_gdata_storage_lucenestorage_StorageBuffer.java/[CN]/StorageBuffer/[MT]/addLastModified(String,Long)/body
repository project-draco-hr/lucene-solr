{
  this.writeLock.lock();
  try {
    if (this.modifiyMap.containsKey(feedId))     this.modifiyMap.remove(feedId);
    this.modifiyMap.put(feedId,timestamp);
  }
  finally {
    this.writeLock.unlock();
  }
}
