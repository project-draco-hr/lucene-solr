{
  assertEquals("it lookups",1,delta("lookups",cache.getStatistics(),statPre));
  assertEquals("it hits",1,delta("hits",cache.getStatistics(),statPre));
  assertEquals("it doesn't insert",0,delta("inserts",cache.getStatistics(),statPre));
}
