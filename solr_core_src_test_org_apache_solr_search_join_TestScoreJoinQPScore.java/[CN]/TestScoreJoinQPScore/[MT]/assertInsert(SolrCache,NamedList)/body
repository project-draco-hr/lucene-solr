{
  assertEquals("it lookups",1,delta("lookups",cache.getStatistics(),statPre));
  assertEquals("it doesn't hit",0,delta("hits",cache.getStatistics(),statPre));
  assertEquals("it inserts",1,delta("inserts",cache.getStatistics(),statPre));
}
