{
  assertEquals("it lookups",1,delta("lookups",cache.getStatistics(),statPre));
  final long mayHit=delta("hits",cache.getStatistics(),statPre);
  assertTrue("it may hit",0 == mayHit || 1 == mayHit);
  assertEquals("or insert on cold",1,delta("inserts",cache.getStatistics(),statPre) + mayHit);
}
