{
  super.init(args);
  minimumLength=getInt(MINIMUM_LENGTH_PARAM,JapaneseKatakanaStemFilter.DEFAULT_MINIMUM_LENGTH);
  if (minimumLength < 2) {
    throw new SolrException(SolrException.ErrorCode.UNKNOWN,"Illegal " + MINIMUM_LENGTH_PARAM + " "+ minimumLength+ " (must be 2 or greater)");
  }
}
