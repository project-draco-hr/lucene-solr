{
  try {
    return hostName + ':' + hostPort+ '_'+ URLEncoder.encode(hostContext,"UTF-8");
  }
 catch (  UnsupportedEncodingException e) {
    throw new IllegalStateException("JVM Does not seem to support UTF-8",e);
  }
}
