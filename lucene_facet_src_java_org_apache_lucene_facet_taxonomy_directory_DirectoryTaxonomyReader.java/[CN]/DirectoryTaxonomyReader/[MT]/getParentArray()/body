{
  ensureOpen();
  if (parentArray == null) {
synchronized (this) {
      if (parentArray == null) {
        parentArray=new ParentArray(indexReader);
      }
    }
  }
  return parentArray.getArray();
}
