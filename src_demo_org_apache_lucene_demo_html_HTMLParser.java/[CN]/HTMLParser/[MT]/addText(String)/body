{
  if (inStyle)   return;
  if (inTitle)   title.append(text);
 else {
    addToSummary(text);
    if (!titleComplete && !title.equals("")) {
synchronized (this) {
        titleComplete=true;
        notifyAll();
      }
    }
  }
  length+=text.length();
  pipeOut.write(text);
  afterSpace=false;
}
