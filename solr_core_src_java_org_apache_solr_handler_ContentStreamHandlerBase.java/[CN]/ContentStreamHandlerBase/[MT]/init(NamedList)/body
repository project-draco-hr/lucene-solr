{
  super.init(args);
  httpCaching=false;
  if (args != null) {
    Object caching=initArgs.get("httpCaching");
    if (caching != null) {
      httpCaching=Boolean.parseBoolean(caching.toString());
    }
  }
}
