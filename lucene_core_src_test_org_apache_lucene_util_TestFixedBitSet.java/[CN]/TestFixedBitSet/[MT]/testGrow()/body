{
  FixedBitSet bits=new FixedBitSet(5);
  bits.set(1);
  bits.set(4);
  FixedBitSet newBits=new FixedBitSet(bits,8);
  assertTrue(newBits.get(1));
  assertTrue(newBits.get(4));
  newBits=new FixedBitSet(bits,72);
  assertTrue(newBits.get(1));
  assertTrue(newBits.get(4));
}
