{
  if (numPending > 0) {
    final DocumentsWriterPerThread dwpt=perThread == null ? null : tryCheckoutForFlush(perThread,false);
    if (dwpt == null) {
      return nextPendingFlush();
    }
    return dwpt;
  }
  return null;
}
