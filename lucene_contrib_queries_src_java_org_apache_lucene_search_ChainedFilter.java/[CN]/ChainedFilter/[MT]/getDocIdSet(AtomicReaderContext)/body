{
  int[] index=new int[1];
  index[0]=0;
  if (logic != -1)   return getDocIdSet(context,logic,index);
 else   if (logicArray != null)   return getDocIdSet(context,logicArray,index);
 else   return getDocIdSet(context,DEFAULT,index);
}
