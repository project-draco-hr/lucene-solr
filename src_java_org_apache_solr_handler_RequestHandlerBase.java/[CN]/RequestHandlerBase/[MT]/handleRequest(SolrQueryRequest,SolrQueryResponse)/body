{
  numRequests++;
  try {
    U.setDefaults(req,defaults,appends,invariants);
    handleRequestBody(req,rsp);
  }
 catch (  SolrException se) {
    numErrors++;
    throw se;
  }
catch (  Exception e) {
    numErrors++;
  }
}
