{
  if (!this.registry.isFeedRegistered(request.getFeedId()))   throw new FeedNotFoundException("Feed could not be found - is not registed - Feed ID:" + request.getFeedId());
  this.storage.setExtensionProfile(request.getExtensionProfile());
}
