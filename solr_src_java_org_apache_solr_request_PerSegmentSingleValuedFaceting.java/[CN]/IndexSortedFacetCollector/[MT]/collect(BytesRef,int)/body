{
  if (count < mincount) {
    return false;
  }
  if (offset > 0) {
    offset--;
    return false;
  }
  if (limit > 0) {
    res.add(term.utf8ToString(),count);
    limit--;
  }
  return limit <= 0;
}
