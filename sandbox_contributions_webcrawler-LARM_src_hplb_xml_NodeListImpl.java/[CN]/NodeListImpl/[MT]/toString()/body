{
  StringBuffer sb=new StringBuffer();
  boolean f=true;
  int count=getLength();
  sb.append("{ ");
  for (int i=0; i < count; i++) {
    if (f) {
      f=false;
    }
 else {
      sb.append(", ");
    }
    sb.append(item(i).toString());
  }
  sb.append(" }");
  return sb.toString();
}
