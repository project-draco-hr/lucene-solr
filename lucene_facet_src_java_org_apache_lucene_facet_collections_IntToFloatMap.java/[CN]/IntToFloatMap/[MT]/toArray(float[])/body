{
  int j=0;
  if (a.length < this.size()) {
    a=new float[this.size()];
  }
  for (FloatIterator iterator=iterator(); iterator.hasNext(); ++j) {
    a[j]=iterator.next();
  }
  if (j < a.length) {
    a[j]=Float.NaN;
  }
  return a;
}
