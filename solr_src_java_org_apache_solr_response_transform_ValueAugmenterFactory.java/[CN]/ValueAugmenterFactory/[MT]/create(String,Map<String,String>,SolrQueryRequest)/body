{
  Object val=value;
  if (val == null) {
    String v=args.get("v");
    if (v == null) {
      val=defaultValue;
    }
 else {
      val=getObjectFrom(v,args.get("t"));
    }
    if (val == null) {
      throw new SolrException(ErrorCode.BAD_REQUEST,"ValueAugmenter is missing a value -- should be defined in solrconfig or inline");
    }
  }
  return new ValueAugmenter(field,val);
}
