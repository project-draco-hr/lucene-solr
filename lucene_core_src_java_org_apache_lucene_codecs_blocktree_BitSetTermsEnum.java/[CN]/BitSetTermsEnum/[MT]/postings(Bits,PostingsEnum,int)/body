{
  if (flags != PostingsEnum.NONE) {
    return null;
  }
  if (liveDocs != null) {
    throw new IllegalArgumentException("cannot handle live docs");
  }
  postingsEnum.reset();
  return postingsEnum;
}
