{
  QueryWrapperFilter qf=new QueryWrapperFilter(query);
  return new SerialChainFilter(new Filter[]{cartesianFilter,qf,distanceFilter},new int[]{SerialChainFilter.AND,SerialChainFilter.AND,SerialChainFilter.SERIALAND});
}
