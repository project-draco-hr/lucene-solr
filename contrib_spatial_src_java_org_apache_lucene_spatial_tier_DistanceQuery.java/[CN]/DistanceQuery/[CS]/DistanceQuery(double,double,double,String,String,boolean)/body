{
  this.lat=lat;
  this.lng=lng;
  this.miles=miles;
  CartesianPolyFilter cpf=new CartesianPolyFilter();
  cartesianFilter=cpf.getBoundingArea(lat,lng,(int)miles);
  if (needPrecise)   distanceFilter=new DistanceFilter(lat,lng,miles,latField,lngField);
}
