{
  final IndexSearcher searcher=acquire();
  try {
    final IndexReader r=searcher.getIndexReader();
    return r instanceof CompositeIndexReader ? ((CompositeIndexReader)r).isCurrent() : true;
  }
  finally {
    release(searcher);
  }
}
