{
  final IndexSearcher searcher=acquire();
  try {
    final IndexReader r=searcher.getIndexReader();
    return r instanceof DirectoryReader ? ((DirectoryReader)r).isCurrent() : true;
  }
  finally {
    release(searcher);
  }
}
