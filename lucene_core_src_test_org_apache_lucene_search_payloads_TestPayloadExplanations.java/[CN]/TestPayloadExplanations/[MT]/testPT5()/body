{
  for (  PayloadFunction fn : functions) {
    SpanQuery q=pt("xx",fn,false);
    q.setBoost(1000);
    qtest(q,new int[]{2,3});
    q=pt("xx",fn,true);
    q.setBoost(1000);
    qtest(q,new int[]{2,3});
  }
}
