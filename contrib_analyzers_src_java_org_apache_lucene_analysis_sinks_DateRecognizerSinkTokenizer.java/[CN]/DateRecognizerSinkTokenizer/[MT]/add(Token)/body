{
  if (t != null) {
    try {
      Date date=dateFormat.parse(new String(t.termBuffer(),0,t.termLength()));
      if (date != null) {
        t.setType(DATE_TYPE);
        lst.add(t.clone());
      }
    }
 catch (    ParseException e) {
    }
  }
}
