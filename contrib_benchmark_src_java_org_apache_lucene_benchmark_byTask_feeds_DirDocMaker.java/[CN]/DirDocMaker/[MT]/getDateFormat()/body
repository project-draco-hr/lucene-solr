{
  DateFormat df=(DateFormat)dateFormat.get();
  if (df == null) {
    df=new SimpleDateFormat("dd-MMM-yyyy kk:mm:ss.SSS",Locale.US);
    df.setLenient(true);
    dateFormat.set(df);
  }
  return df;
}
