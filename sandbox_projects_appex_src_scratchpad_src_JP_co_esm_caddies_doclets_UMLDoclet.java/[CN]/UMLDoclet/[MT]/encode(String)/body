{
  StringBuffer res=new StringBuffer();
  for (int i=0; i < s.length(); i++) {
    char c=s.charAt(i);
    if (c == '>')     res.append("&gt;");
 else     if (c == '<')     res.append("&lt;");
 else     res.append(c);
  }
  return res.toString();
}
