{
  int maxNumElements=10000;
  String text=_TestUtil.randomHtmlishString(random,maxNumElements);
  Reader reader=new HTMLStripCharFilter(CharReader.get(new StringReader(text)));
  while (reader.read() != -1)   ;
}
