{
  assert(jetty != null);
  monkeyLog("stop shard! " + jetty.getLocalPort());
  SolrDispatchFilter sdf=jetty.getSolrDispatchFilter();
  if (sdf != null)   sdf.destroy();
  jetty.stop();
  if (!jetty.isStopped()) {
    throw new RuntimeException("could not stop jetty");
  }
}
