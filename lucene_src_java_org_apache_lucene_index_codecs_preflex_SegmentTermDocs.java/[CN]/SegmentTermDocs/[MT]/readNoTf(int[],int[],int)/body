{
  int i=0;
  while (i < length && count < df) {
    doc+=freqStream.readVInt();
    count++;
    if (skipDocs == null || !skipDocs.get(doc)) {
      docs[i]=doc;
      freqs[i]=1;
      ++i;
    }
  }
  return i;
}
