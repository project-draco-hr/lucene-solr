{
  Analyzer a=new IrishAnalyzer(TEST_VERSION_CURRENT);
  checkOneTermReuse(a,"siopad??ireacht","siopad??ir");
  checkOneTermReuse(a,"s??ceapatacha","s??ceapaite");
  assertAnalyzesTo(a,"le",new String[]{});
}
