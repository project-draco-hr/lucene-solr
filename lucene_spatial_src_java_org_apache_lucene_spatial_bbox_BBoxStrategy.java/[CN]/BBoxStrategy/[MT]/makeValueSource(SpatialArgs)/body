{
  Shape shape=args.getShape();
  if (!(shape instanceof Rectangle))   throw new IllegalArgumentException("Can only get valueSource by Rectangle, not " + shape);
  return new BBoxSimilarityValueSource(this,new AreaSimilarity((Rectangle)shape,queryPower,targetPower));
}
