{
  if (value == 1) {
    if (++onesCounter == n) {
      super.encode(2);
      onesCounter=0;
    }
    return;
  }
  while (onesCounter > 0) {
    --onesCounter;
    super.encode(1);
  }
  super.encode(value + 1);
}
