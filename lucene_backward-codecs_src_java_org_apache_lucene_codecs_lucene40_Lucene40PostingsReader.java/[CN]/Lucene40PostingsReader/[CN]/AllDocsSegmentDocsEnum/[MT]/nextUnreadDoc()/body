{
  if (ord++ < limit) {
    int code=freqIn.readVInt();
    if (indexOmitsTF) {
      accum+=code;
    }
 else {
      accum+=code >>> 1;
      freq=readFreq(freqIn,code);
    }
    return accum;
  }
 else {
    return NO_MORE_DOCS;
  }
}
