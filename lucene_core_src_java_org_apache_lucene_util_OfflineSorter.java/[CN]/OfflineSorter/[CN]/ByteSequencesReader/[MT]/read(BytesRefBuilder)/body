{
  short length;
  try {
    length=in.readShort();
  }
 catch (  EOFException e) {
    return false;
  }
  ref.grow(length);
  ref.setLength(length);
  in.readBytes(ref.bytes(),0,length);
  return true;
}
