{
  ZkNodeProps props=new ZkNodeProps();
  props.put(ZkStateReader.URL_PROP,url);
  props.put(ZkStateReader.NODE_NAME,zkNodeName);
  byte[] bytes=props.store();
  zkClient.setData(shardsPath + "/" + zkNodeName,bytes);
}
