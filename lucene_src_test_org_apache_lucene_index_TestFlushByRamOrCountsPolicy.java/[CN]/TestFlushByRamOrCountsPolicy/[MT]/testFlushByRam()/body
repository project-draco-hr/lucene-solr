{
  int[] numThreads=new int[]{3 + random.nextInt(12),1};
  for (int i=0; i < numThreads.length; i++) {
    runFlushByRam(numThreads[i],1 + random.nextInt(10) + random.nextDouble(),false);
  }
  for (int i=0; i < numThreads.length; i++) {
    runFlushByRam(numThreads[i],512.d,true);
  }
}
