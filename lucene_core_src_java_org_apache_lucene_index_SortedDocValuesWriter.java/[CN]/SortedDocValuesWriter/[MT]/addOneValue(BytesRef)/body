{
  int termID=hash.add(value);
  if (termID < 0) {
    termID=-termID - 1;
  }
 else {
    iwBytesUsed.addAndGet(2 * RamUsageEstimator.NUM_BYTES_INT);
  }
  pending.add(termID);
  updateBytesUsed();
}
