{
  while (true) {
    docID++;
    if (docID >= maxDoc) {
      docID=NO_MORE_DOCS;
      break;
    }
    ord=(int)iter.next();
    if (ord != -1) {
      ord=ordMap[ord];
      break;
    }
  }
  return docID;
}
