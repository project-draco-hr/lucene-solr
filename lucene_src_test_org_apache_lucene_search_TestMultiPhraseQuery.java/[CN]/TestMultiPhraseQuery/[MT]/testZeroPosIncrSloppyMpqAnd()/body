{
  final MultiPhraseQuery mpq=new MultiPhraseQuery();
  for (  TokenAndPos tap : INCR_0_QUERY_TOKENS_AND) {
    mpq.add(new Term[]{new Term("field",tap.token)},tap.pos);
  }
  doTestZeroPosIncrSloppy(mpq,0);
  mpq.setSlop(1);
  doTestZeroPosIncrSloppy(mpq,0);
  mpq.setSlop(2);
  doTestZeroPosIncrSloppy(mpq,1);
}
