{
  if (numeric()) {
    return new NumericField(name()).setIntValue(counter).tokenStream(analyzer);
  }
  return readerValue() != null ? analyzer.reusableTokenStream(name(),readerValue()) : analyzer.reusableTokenStream(name(),new StringReader(stringValue()));
}
