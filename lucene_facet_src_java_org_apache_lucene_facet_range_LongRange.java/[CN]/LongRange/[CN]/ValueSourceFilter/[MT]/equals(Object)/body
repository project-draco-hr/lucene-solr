{
  if (super.equals(obj) == false) {
    return false;
  }
  ValueSourceFilter other=(ValueSourceFilter)obj;
  return range.equals(other.range) && Objects.equals(fastMatchFilter,other.fastMatchFilter) && valueSource.equals(other.valueSource);
}
