{
  if (super.equals(o) == false) {
    return false;
  }
  ReRankQuery rrq=(ReRankQuery)o;
  return mainQuery.equals(rrq.mainQuery) && reRankQuery.equals(rrq.reRankQuery) && reRankWeight == rrq.reRankWeight && reRankDocs == rrq.reRankDocs;
}
