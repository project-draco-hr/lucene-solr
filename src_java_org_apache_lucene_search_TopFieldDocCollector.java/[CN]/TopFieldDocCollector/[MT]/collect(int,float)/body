{
  if (score > 0.0f) {
    totalHits++;
    if (reusableFD == null)     reusableFD=new FieldDoc(doc + docBase,score);
 else {
      reusableFD.score=score;
      reusableFD.doc=doc + docBase;
    }
    reusableFD=(FieldDoc)hq.insertWithOverflow(reusableFD);
  }
}
