{
  if (random().nextBoolean()) {
    return new FunctionQuery(valueSource);
  }
 else {
    Integer lower=(random().nextBoolean() ? null : 1);
    Integer upper=(random().nextBoolean() ? null : N_DOCS);
    return new FunctionRangeQuery(valueSource,lower,upper,true,true);
  }
}
