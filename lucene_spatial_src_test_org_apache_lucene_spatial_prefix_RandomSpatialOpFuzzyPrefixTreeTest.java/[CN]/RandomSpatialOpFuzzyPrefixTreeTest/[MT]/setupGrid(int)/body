{
  if (randomBoolean())   setupQuadGrid(maxLevels);
 else   setupGeohashGrid(maxLevels);
  setupCtx2D(ctx);
  ((RecursivePrefixTreeStrategy)strategy).setPruneLeafyBranches(randomBoolean());
  System.out.println("Strategy: " + strategy.toString());
}
