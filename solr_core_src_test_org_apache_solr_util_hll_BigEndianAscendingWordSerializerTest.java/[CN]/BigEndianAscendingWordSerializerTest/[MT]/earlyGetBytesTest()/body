{
  final BigEndianAscendingWordSerializer serializer=new BigEndianAscendingWordSerializer(5,1,0);
  try {
    serializer.getBytes();
    fail("Should throw.");
  }
 catch (  final RuntimeException e) {
    assertTrue(e.getMessage().contains("Not all words"));
  }
}
