{
  final CollectionAdminRequest.DeleteReplica request=new CollectionAdminRequest.DeleteReplica().setCollectionName("collection").setShardName("shard").setReplica("replica");
  assertContainsParams(request.getParams(),ACTION,COLLECTION,SHARD,REPLICA);
}
