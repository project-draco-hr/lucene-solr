{
  return new Docs(){
    private final FixedBitSet bits=new FixedBitSet(maxDoc);
    @Override public void addDoc(    int docId) throws IOException {
      bits.set(docId);
    }
    @Override public DocIdSet getDocIdSet(){
      return new FixedBitDocIdSet(bits);
    }
  }
;
}
