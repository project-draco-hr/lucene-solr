{
  int numIters=atLeast(2);
  for (int i=0; i < numIters; i++) {
    doTestGeneralSynchronization(_TestUtil.nextInt(random(),2,4),random().nextBoolean() ? -1 : _TestUtil.nextInt(random(),1,10),_TestUtil.nextInt(random(),0,3));
  }
}
