{
  if (valSize == 0)   throw new IllegalStateException("call set???Value() before usage");
  if (precisionStep < 1 || precisionStep > valSize)   throw new IllegalArgumentException("precisionStep may only be 1.." + valSize);
  shift=0;
}
