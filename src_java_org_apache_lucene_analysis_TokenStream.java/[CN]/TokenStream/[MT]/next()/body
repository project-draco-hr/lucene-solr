{
  Token result=next(new Token());
  if (result != null) {
    Payload p=result.getPayload();
    if (p != null)     result.setPayload(new Payload(p.toByteArray(),0,p.length()));
  }
  return result;
}
