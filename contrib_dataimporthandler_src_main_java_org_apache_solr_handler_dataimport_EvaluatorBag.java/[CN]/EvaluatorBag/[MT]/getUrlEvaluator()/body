{
  return new Evaluator(){
    public String evaluate(    VariableResolver resolver,    String expression){
      Object value=null;
      try {
        value=resolver.resolve(expression);
        if (value == null)         return null;
        return URLEncoder.encode(value.toString(),"UTF-8");
      }
 catch (      UnsupportedEncodingException e) {
        throw new DataImportHandlerException(DataImportHandlerException.SEVERE,"Unable to encode expression: " + expression + " with value: "+ value,e);
      }
    }
  }
;
}
