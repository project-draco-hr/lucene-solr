{
  if (now == null) {
    SolrRequestInfo reqInfo=SolrRequestInfo.getRequestInfo();
    if (reqInfo == null) {
      now=new Date();
    }
 else {
      now=reqInfo.getNOW();
    }
  }
  return (Date)now.clone();
}
