{
  List<String> filenames=new ArrayList<String>();
  IndexFileNameFilter filter=IndexFileNameFilter.getFilter();
  for (  String name : listAll())   if (filter.accept(null,name))   filenames.add(name);
  copyTo(to,filenames);
}
