{
  IOException ioe=null;
  for (int i=0; i < subReaders.length; i++) {
    try {
      if (closeSubReaders) {
        subReaders[i].close();
      }
 else {
        subReaders[i].decRef();
      }
    }
 catch (    IOException e) {
      if (ioe == null)       ioe=e;
    }
  }
  if (ioe != null)   throw ioe;
}
