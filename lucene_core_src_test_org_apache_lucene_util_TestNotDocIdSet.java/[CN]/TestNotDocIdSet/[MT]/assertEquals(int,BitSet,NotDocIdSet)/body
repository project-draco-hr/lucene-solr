{
  super.assertEquals(numBits,ds1,ds2);
  final Bits bits2=ds2.bits();
  assertTrue(ds2.isCacheable());
  assertNotNull(bits2);
  assertEquals(numBits,bits2.length());
  for (int i=0; i < numBits; ++i) {
    assertEquals(ds1.get(i),bits2.get(i));
  }
}
