{
  super.setUp();
  assertNull("UniqueKey Field isn't null",h.getCore().getSchema().getUniqueKeyField());
  lrf.args.put("version","2.0");
  assertU("Simple assertion that adding a document works",adoc("id","4055","subject","Hoss","project","Solr"));
  assertU(adoc("id","4056","subject","Yonik","project","Solr"));
  assertU(commit());
  assertU(optimize());
}
