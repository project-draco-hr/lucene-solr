{
  if (null == name)   return RussianCharsets.UnicodeRussian;
  char[] charset=CHARSETS.get(name);
  if (null == charset) {
    throw new SolrException(ErrorCode.SERVER_ERROR,"Don't understand charset: " + name);
  }
  return charset;
}
