{
  this.reader=reader;
  this.tindex=tindex;
  this.pos=pos;
  Terms terms=MultiFields.getTerms(reader,tindex.field);
  if (terms != null) {
    tenum=terms.iterator();
    tenum.seek(termValue);
    setTerm();
  }
}
