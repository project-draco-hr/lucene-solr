{
  int len=Math.min(Math.min((int)(size - offset),b.length),maxLen);
  if (len > 0) {
    len=pageFileIS.read(b,off,maxLen);
    if (len != -1) {
      offset+=len;
    }
    return len;
  }
  return -1;
}
