{
  if (lst == null) {
    lst=new LinkedList();
    for (; ; ) {
      Token t=input.next();
      if (t == null)       break;
      lst.add(t);
    }
  }
  return lst.size() == 0 ? null : (Token)lst.remove(0);
}
