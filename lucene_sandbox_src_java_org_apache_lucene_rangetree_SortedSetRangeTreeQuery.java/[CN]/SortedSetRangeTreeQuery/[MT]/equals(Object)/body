{
  if (super.equals(other)) {
    final SortedSetRangeTreeQuery q=(SortedSetRangeTreeQuery)other;
    return ((q.minValue == null ? minValue == null : q.minValue.equals(minValue)) && (q.maxValue == null ? maxValue == null : q.maxValue.equals(maxValue)) && minInclusive == q.minInclusive && maxInclusive == q.maxInclusive);
  }
  return false;
}
