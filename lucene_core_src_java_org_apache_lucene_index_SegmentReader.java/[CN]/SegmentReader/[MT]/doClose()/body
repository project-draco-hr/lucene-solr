{
  try {
    core.decRef();
  }
  finally {
    dvProducers.clear();
    try {
      IOUtils.close(docValuesLocal,docsWithFieldLocal);
    }
  finally {
      segDocValues.decRef(dvGens);
    }
  }
}
