{
  SpanNearQuery q=new SpanNearQuery(new SpanQuery[]{new SpanTermQuery(new Term(FIELD,"t2")),new SpanTermQuery(new Term(FIELD,"t1"))},1,true);
  Spans spans=MultiSpansWrapper.wrap(reader,q);
  assertNext(spans,4,1,4);
  assertNext(spans,4,2,4);
  assertFinished(spans);
}
