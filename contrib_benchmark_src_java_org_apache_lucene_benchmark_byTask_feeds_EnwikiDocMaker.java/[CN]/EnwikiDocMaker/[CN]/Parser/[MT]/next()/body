{
  if (t == null) {
    t=new Thread(this);
    t.setDaemon(true);
    t.start();
  }
  String[] result;
synchronized (this) {
    while (tuple == null && nmde == null) {
      try {
        wait();
      }
 catch (      InterruptedException ie) {
      }
    }
    if (nmde != null) {
      t=null;
      throw nmde;
    }
    result=tuple;
    tuple=null;
    notify();
  }
  return result;
}
