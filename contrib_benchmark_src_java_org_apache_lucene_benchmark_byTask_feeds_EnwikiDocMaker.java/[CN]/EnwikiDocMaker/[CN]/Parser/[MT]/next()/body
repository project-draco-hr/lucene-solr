{
  String[] result;
synchronized (this) {
    while (tuple == null && nmde == null) {
      try {
        wait();
      }
 catch (      InterruptedException ie) {
      }
    }
    if (nmde != null) {
      throw nmde;
    }
    result=tuple;
    tuple=null;
    notify();
  }
  return result;
}
