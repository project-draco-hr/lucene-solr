{
  if (table.contains("@")) {
    String[] parts=table.split("@");
    this.collection=parts[0];
    this.zkHost=parts[1];
  }
 else {
    this.collection=table;
    this.zkHost=defaultZkHost;
  }
}
