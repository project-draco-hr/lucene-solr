{
  String v=args.remove(name);
  if (v != null) {
    if (v.length() != 1) {
      throw new IllegalArgumentException(name + " should be a char. \"" + v+ "\" is invalid");
    }
 else {
      return v.charAt(0);
    }
  }
 else {
    return defaultValue;
  }
}
