{
  if (super.equals(obj) == false) {
    return false;
  }
  BoostingQuery that=(BoostingQuery)obj;
  return match.equals(that.match) && context.equals(that.context) && Float.floatToIntBits(boost) == Float.floatToIntBits(that.boost);
}
