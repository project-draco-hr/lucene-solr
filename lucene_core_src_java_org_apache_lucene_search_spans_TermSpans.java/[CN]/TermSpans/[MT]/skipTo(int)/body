{
  doc=postings.advance(target);
  if (doc == DocsAndPositionsEnum.NO_MORE_DOCS) {
    return false;
  }
  freq=postings.freq();
  count=0;
  position=postings.nextPosition();
  count++;
  return true;
}
