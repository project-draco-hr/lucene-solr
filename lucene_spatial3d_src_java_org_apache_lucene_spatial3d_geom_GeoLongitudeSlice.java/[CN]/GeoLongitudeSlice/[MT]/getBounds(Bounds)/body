{
  super.getBounds(bounds);
  bounds.addVerticalPlane(planetModel,leftLon,leftPlane,rightPlane).addVerticalPlane(planetModel,rightLon,rightPlane,leftPlane).addPoint(planetModel.NORTH_POLE).addPoint(planetModel.SOUTH_POLE);
}
