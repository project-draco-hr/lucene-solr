{
  List<ValueSource> sources=new ArrayList<ValueSource>(3);
  for (; ; ) {
    sources.add(parseValSource());
    char ch=sp.peek();
    if (ch == ')')     break;
    sp.expect(",");
  }
  return sources;
}
