{
  return new FieldHighlighter(field,getOffsetStrategy(field,query,allTerms),new SplittingBreakIterator(getBreakIterator(field),UnifiedHighlighter.MULTIVAL_SEP_CHAR),getScorer(field),maxPassages,getMaxNoHighlightPassages(field),getFormatter(field));
}
