{
  if (absolute) {
    upto=indexIn.readVInt();
    fp=indexIn.readVLong();
  }
 else {
    final int uptoDelta=indexIn.readVInt();
    if ((uptoDelta & 1) == 1) {
      upto+=uptoDelta >>> 1;
    }
 else {
      upto=uptoDelta >>> 1;
      fp+=indexIn.readVLong();
    }
  }
}
