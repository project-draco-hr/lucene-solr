{
  this.solrConfig=solrConfig;
  if (name == null)   name=DEFAULT_SCHEMA_FILE;
  this.resourceName=name;
  loader=solrConfig.getResourceLoader();
  InputStream lis=is;
  if (lis == null)   lis=loader.openSchema(name);
  readSchema(lis);
  if (lis != is) {
    try {
      lis.close();
    }
 catch (    IOException xio) {
    }
  }
  loader.inform(loader);
}
