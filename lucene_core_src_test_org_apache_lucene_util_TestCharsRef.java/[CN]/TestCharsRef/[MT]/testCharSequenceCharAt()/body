{
  CharsRef c=new CharsRef("abc");
  assertEquals('b',c.charAt(1));
  try {
    c.charAt(-1);
    fail();
  }
 catch (  IndexOutOfBoundsException expected) {
  }
  try {
    c.charAt(3);
    fail();
  }
 catch (  IndexOutOfBoundsException expected) {
  }
}
