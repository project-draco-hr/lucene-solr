{
  char chars[]=new char[]{'a','b','c','d'};
  CharsRef c=new CharsRef(chars,1,3);
  char otherchars[]=new char[]{'b','c','d','e'};
  c.copyChars(new CharsRef(otherchars,0,4));
  assertEquals("bcde",c.toString());
}
