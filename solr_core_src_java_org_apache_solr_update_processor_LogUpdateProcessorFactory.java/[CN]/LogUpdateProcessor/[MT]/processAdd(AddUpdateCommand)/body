{
  if (adds == null) {
    adds=new ArrayList<String>();
    toLog.add("add",adds);
  }
  if (adds.size() < maxNumToLog) {
    adds.add(cmd.getPrintableId());
  }
  if (logDebug) {
    log.debug("add {}",cmd.getPrintableId());
  }
  numAdds++;
  if (next != null)   next.processAdd(cmd);
}
