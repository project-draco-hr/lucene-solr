{
  PackedInts.Reader indexReader=indexHeader == null ? null : PackedInts.getReaderNoHeader(indexIn.clone(),indexHeader);
  PagedBytes bytes=new PagedBytes(15);
  bytes.copy(datIn.clone(),indexReader == null ? size * valueCount : indexReader.get(indexReader.size() - 1));
  bytes.freeze(true);
  return new Lucene41BinaryDocValues(bytes.getDataInput(),0,size,maxLength,indexReader,null);
}
