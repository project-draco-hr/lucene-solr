{
  if (input.incrementToken()) {
    if (!keywordAtt.isKeyword())     termAtt.setTermLength(stemmer.stem(termAtt.termBuffer(),termAtt.termLength()));
    return true;
  }
 else {
    return false;
  }
}
