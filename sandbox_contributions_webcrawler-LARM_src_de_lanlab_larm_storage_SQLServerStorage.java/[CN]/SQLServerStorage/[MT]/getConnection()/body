{
synchronized (this) {
    Connection actual=(Connection)freeCons.firstElement();
    freeCons.removeElementAt(0);
    if (actual == null) {
      return null;
    }
    busyCons.add(actual);
    return actual;
  }
}
