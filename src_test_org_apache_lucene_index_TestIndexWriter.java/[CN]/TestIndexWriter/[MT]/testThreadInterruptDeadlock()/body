{
  IndexerThreadInterrupt t=new IndexerThreadInterrupt();
  t.setDaemon(true);
  t.start();
  for (int i=0; i < 100; i++) {
    Thread.sleep(1);
synchronized (t) {
      t.interrupt();
    }
  }
  t.finish=true;
  t.interrupt();
  t.join();
  assertFalse(t.failed);
}
