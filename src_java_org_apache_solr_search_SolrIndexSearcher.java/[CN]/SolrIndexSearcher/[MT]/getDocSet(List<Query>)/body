{
  DocSet answer=null;
  if (queries == null)   return null;
  for (  Query q : queries) {
    if (answer == null) {
      answer=getDocSet(q);
    }
 else {
      answer=answer.intersection(getDocSet(q));
    }
  }
  return answer;
}
