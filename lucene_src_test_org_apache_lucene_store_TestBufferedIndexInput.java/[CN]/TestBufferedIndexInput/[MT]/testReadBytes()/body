{
  MyBufferedIndexInput input=new MyBufferedIndexInput();
  runReadBytes(input,BufferedIndexInput.BUFFER_SIZE,random);
  final int inputBufferSize=128;
  File tmpInputFile=_TestUtil.createTempFile("IndexInput","tmpFile",TEMP_DIR);
  tmpInputFile.deleteOnExit();
  writeBytes(tmpInputFile,TEST_FILE_LENGTH);
  runReadBytesAndClose(new SimpleFSIndexInput(tmpInputFile,IOContext.DEFAULT,10),inputBufferSize,random);
  runReadBytesAndClose(new NIOFSIndexInput(tmpInputFile,IOContext.DEFAULT,10),inputBufferSize,random);
}
