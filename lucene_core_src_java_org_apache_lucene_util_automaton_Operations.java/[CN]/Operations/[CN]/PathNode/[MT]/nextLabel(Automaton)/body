{
  if (label > t.max) {
    transition++;
    if (transition >= a.getNumTransitions(state)) {
      return -1;
    }
    a.getTransition(state,transition,t);
    label=t.min;
    to=t.dest;
  }
  return label++;
}
