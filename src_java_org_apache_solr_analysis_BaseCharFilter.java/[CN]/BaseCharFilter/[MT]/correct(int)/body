{
  if (pcmList == null || pcmList.isEmpty())   return currentOff;
  for (int i=pcmList.size() - 1; i >= 0; i--) {
    if (currentOff >= pcmList.get(i).off)     return currentOff + pcmList.get(i).cumulativeDiff;
  }
  return currentOff;
}
