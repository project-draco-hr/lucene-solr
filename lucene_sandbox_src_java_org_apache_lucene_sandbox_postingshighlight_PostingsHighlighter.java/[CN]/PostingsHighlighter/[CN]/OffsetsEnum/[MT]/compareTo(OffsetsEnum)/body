{
  try {
    int off=dp.startOffset();
    int otherOff=other.dp.startOffset();
    if (off == otherOff) {
      return id - other.id;
    }
 else {
      return off - otherOff;
    }
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
}
