{
  hasVectors=false;
  try {
    super.abort();
  }
  finally {
    if (writer != null) {
      IOUtils.closeWhileHandlingException(writer);
      writer=null;
    }
    lastDocID=0;
    reset();
  }
}
