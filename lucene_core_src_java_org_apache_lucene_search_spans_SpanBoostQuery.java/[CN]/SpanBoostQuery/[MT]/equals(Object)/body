{
  if (super.equals(obj) == false) {
    return false;
  }
  SpanBoostQuery that=(SpanBoostQuery)obj;
  return query.equals(that.query) && Float.floatToIntBits(boost) == Float.floatToIntBits(that.boost);
}
