{
  RangeQuery rangeQuery=new RangeQuery(field,part1 == null ? null : toInternal(part1),part2 == null ? null : toInternal(part2),minInclusive,maxInclusive);
  rangeQuery.setConstantScoreRewrite(true);
  return rangeQuery;
}
