{
  RangeQuery rangeQuery=new RangeQuery(field,"*".equals(part1) ? null : toInternal(part1),"*".equals(part2) ? null : toInternal(part2),inclusive,inclusive);
  rangeQuery.setConstantScoreRewrite(true);
  return rangeQuery;
}
