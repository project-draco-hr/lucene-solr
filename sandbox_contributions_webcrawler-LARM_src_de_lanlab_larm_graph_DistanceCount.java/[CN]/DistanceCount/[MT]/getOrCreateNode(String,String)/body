{
  Node node=(Node)nodes.get(name);
  if (node != null) {
    if (title != null) {
      node.setTitle(title);
    }
    return node;
  }
 else {
    node=new Node(id++,name,title);
    nodes.put(name,node);
    return node;
  }
}
