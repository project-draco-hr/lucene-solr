{
  setupDocs();
  int docId=1;
  int count=1;
  boolean needScore=random().nextBoolean();
  assertQ(req("fl","id","q","*:*","rows","1000","fq","{! needScore=" + needScore + " df="+ fieldName+ "}[32,-80 TO 33,-79]"),"//result/doc/*[@name='id'][.='" + docId + "']","*[count(//doc)=" + count + "]");
}
