{
  assertEquals(Long.MIN_VALUE,NumberTools.stringToLong(NumberTools.MIN_STRING_VALUE));
  assertEquals(NumberTools.MIN_STRING_VALUE,NumberTools.longToString(Long.MIN_VALUE));
  for (long l=Long.MIN_VALUE; l < Long.MIN_VALUE + 10000; l++) {
    subtestTwoLongs(l,l + 1);
  }
}
