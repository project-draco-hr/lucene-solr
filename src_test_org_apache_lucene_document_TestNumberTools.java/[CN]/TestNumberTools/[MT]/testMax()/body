{
  assertEquals(Long.MAX_VALUE,NumberTools.stringToLong(NumberTools.MAX_STRING_VALUE));
  assertEquals(NumberTools.MAX_STRING_VALUE,NumberTools.longToString(Long.MAX_VALUE));
  for (long l=Long.MAX_VALUE; l > Long.MAX_VALUE - 10000; l--) {
    subtestTwoLongs(l,l - 1);
  }
}
