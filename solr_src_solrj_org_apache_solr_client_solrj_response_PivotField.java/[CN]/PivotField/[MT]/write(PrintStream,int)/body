{
  for (int i=0; i < indent; i++) {
    out.print("  ");
  }
  out.println(_field + "=" + _value+ " ("+ _count+ ")");
  if (_pivot != null) {
    for (    PivotField p : _pivot) {
      p.write(out,indent + 1);
    }
  }
}
