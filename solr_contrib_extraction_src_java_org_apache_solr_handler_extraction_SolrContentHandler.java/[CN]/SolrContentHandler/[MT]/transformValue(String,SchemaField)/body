{
  String result=val;
  if (schFld != null && schFld.getType() instanceof TrieDateField) {
    try {
      Date date=ExtractionDateUtil.parseDate(val,dateFormats);
      result=date.toInstant().toString();
    }
 catch (    Exception e) {
    }
  }
  return result;
}
