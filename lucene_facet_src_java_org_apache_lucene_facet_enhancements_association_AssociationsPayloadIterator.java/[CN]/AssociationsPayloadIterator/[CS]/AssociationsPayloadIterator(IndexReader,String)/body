{
  associationPayloadIter=new PayloadIntDecodingIterator(reader,new Term(field,AssociationEnhancement.CATEGORY_LIST_TERM_TEXT),new SimpleIntDecoder());
  hasAssociations=associationPayloadIter.init();
  ordinalToAssociationMap=new IntToIntMap();
}
