{
  if (bufferUpto != -1) {
    if (bufferUpto > 0)     docWriter.ramAllocator.recycleIntBlocks(buffers,1,1 + bufferUpto);
    bufferUpto=0;
    intUpto=0;
    intOffset=0;
    buffer=buffers[0];
  }
}
