{
  boolean distint=false;
  if (_removeDuplicates != null) {
    Boolean v=_removeDuplicates.get(name);
    if (v == null) {
      v=_removeDuplicates.get(null);
    }
    distint=(v == Boolean.TRUE);
  }
  return distint ? new LinkedHashSet<Object>() : new ArrayList<Object>(1);
}
