{
  String queryString=context.getEntityAttribute(QUERY);
  if (context.currentProcess() == Context.FULL_DUMP || !context.isRootEntity()) {
    return queryString;
  }
  return getDeltaImportQuery(queryString);
}
