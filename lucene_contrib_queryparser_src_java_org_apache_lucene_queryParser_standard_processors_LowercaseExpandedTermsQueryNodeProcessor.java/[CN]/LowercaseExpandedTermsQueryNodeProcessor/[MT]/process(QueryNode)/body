{
  if (getQueryConfigHandler().hasAttribute(LowercaseExpandedTermsAttribute.class)) {
    if (getQueryConfigHandler().getAttribute(LowercaseExpandedTermsAttribute.class).isLowercaseExpandedTerms()) {
      return super.process(queryTree);
    }
  }
  return queryTree;
}
