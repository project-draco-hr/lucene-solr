{
  for (  String s : l) {
    DeleteUpdateCommand delcmd=new DeleteUpdateCommand(req);
    if (isId) {
      delcmd.id=s;
    }
 else {
      delcmd.query=s;
    }
    processor.processDelete(delcmd);
  }
}
