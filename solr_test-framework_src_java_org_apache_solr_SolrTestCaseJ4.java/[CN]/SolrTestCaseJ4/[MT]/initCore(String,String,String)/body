{
  assertNotNull(solrHome);
  configString=config;
  schemaString=schema;
  testSolrHome=Paths.get(solrHome);
  if (solrHome != null) {
    System.setProperty("solr.solr.home",solrHome);
  }
  initCore();
}
