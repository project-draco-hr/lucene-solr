{
switch (random.nextInt(3)) {
case 0:
    return new SimpleAnalyzer(TEST_VERSION_CURRENT);
case 1:
  return new StopAnalyzer(TEST_VERSION_CURRENT);
default :
return new StandardAnalyzer(TEST_VERSION_CURRENT);
}
}
