{
  ZkStateReader mock=createMock(ZkStateReader.class);
  EasyMock.reset(mock);
  mock.getAllCollections();
  EasyMock.expectLastCall().andAnswer(new IAnswer<Set<String>>(){
    @Override public Set<String> answer() throws Throwable {
      return collections;
    }
  }
).anyTimes();
  EasyMock.replay(mock);
  return mock;
}
