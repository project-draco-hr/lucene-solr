def __errcheck(self, rsp):
    if (rsp.status != 200):
        ex = SolrException(rsp.status)
        try:
            ex.body = rsp.read()
        except:
            pass
        raise ex
    return rsp
