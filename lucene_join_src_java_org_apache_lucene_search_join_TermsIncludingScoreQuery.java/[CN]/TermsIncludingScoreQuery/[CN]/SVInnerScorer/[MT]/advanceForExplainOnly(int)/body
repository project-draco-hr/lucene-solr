{
  int docId;
  do {
    docId=nextDoc();
    if (docId < target) {
      int tempDocId=docsEnum.advance(target);
      if (tempDocId == target) {
        docId=tempDocId;
        break;
      }
    }
 else     if (docId == target) {
      break;
    }
    docsEnum=null;
  }
 while (docId != DocIdSetIterator.NO_MORE_DOCS);
  return docId;
}
