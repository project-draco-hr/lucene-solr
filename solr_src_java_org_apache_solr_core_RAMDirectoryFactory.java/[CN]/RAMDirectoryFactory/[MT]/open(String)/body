{
synchronized (this) {
    Directory directory=directories.get(path);
    if (directory == null) {
      directory=openNew(path);
      directories.put(path,directory);
    }
    return directory;
  }
}
