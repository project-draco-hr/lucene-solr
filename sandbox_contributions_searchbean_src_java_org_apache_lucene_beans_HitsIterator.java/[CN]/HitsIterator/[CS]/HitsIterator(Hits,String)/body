{
  this.hitsCollection=hits;
  if (sortFlag != null) {
    if ((sortFlag != "") && (sortFlag != "relevance")) {
      sortByField(sortFlag);
    }
  }
  totalHits=getTotalHits();
  setPageCount();
}
