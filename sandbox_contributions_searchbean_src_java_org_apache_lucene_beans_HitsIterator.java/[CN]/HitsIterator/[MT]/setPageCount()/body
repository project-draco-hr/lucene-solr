{
  if (totalHits == 0) {
    totalPages=0;
    setCurrentPage(0);
  }
 else {
    totalPages=totalHits / pageSize;
    if (totalHits % pageSize != 0) {
      totalPages++;
    }
    setCurrentPage(1);
  }
}
