{
  if (arrayOfIndividualHits == null)   return (int)(hitsCollection.score(currentPosition - 1) * 100.0f);
 else   return (int)(((IndividualHit)arrayOfIndividualHits[currentPosition - 1]).getScore() * 100.0f);
}
