{
  while (pauseThreads != 0 || flushPending)   try {
    wait();
  }
 catch (  InterruptedException e) {
  }
  for (int i=0; i < terms.length; i++)   addDeleteTerm(terms[i],numDocsInRAM);
  return timeToFlushDeletes();
}
