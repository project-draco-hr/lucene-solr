{
  int len=(token >>> 4) & 0x07;
  final int startPosition=in.getPosition();
  if ((len & 0x04) != 0) {
    len=(len & 0x03) | (in.readVInt() << 2);
  }
  if (startPosition != 1) {
    len+=2;
  }
  return len;
}
