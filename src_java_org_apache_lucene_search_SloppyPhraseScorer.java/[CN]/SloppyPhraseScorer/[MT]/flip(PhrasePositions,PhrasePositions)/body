{
  int n=0;
  PhrasePositions pp3;
  while ((pp3=(PhrasePositions)pq.pop()) != pp2) {
    tmpPos[n++]=pp3;
  }
  for (n--; n >= 0; n--) {
    pq.insertWithOverflow(tmpPos[n]);
  }
  pq.add(pp);
  return pp2;
}
