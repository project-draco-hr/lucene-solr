{
  while (currentUpperBound == null || termComp.compare(term,currentUpperBound) > 0) {
    if (rangeBounds.isEmpty())     return AcceptStatus.END;
    if (termComp.compare(term,rangeBounds.getFirst()) < 0)     return AcceptStatus.NO_AND_SEEK;
    nextRange();
  }
  return AcceptStatus.YES;
}
