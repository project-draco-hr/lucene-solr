{
  if (path.intersects(topPlane,bottomPlane) || path.intersects(bottomPlane,topPlane))   return OVERLAPS;
  if (path.isWithin(interiorPoint))   return CONTAINS;
  if (isWithin(path.getInteriorPoint()))   return WITHIN;
  return DISJOINT;
}
