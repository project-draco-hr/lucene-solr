{
  if (t != null) {
    try {
      Date date=dateFormat.parse(t.term());
      if (date != null) {
        t.setType(DATE_TYPE);
        super.add(t);
      }
    }
 catch (    ParseException e) {
    }
  }
}
