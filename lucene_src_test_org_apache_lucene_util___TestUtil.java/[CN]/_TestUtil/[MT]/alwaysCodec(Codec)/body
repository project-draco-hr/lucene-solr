{
  CodecProvider p=new CodecProvider(){
    @Override public Codec lookup(    String name){
      if (name.equals(c.name)) {
        return c;
      }
 else {
        return CodecProvider.getDefault().lookup(name);
      }
    }
  }
;
  p.setDefaultFieldCodec(c.name);
  return p;
}
