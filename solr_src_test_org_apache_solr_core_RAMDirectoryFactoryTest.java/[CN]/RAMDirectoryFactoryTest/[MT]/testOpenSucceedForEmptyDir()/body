{
  RAMDirectoryFactory factory=new RAMDirectoryFactory();
  Directory dir=factory.open("/fake/path");
  assertNotNull("RAMDirectoryFactory should create RAMDirectory even if the path doen't lead " + "to index directory on the file system",dir);
}
