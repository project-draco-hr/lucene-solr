{
synchronized (cores) {
    for (    Map.Entry<String,CoreDescriptor> e : cores.entrySet()) {
      SolrCore core=e.getValue().getCore();
      if (core == null)       continue;
      String key=e.getKey();
      if (core.getName().equals(key))       core.close();
    }
    cores.clear();
  }
}
