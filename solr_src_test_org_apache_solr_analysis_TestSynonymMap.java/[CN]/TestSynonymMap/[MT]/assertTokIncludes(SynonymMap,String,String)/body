{
  Token[] tokens=((SynonymMap)map.submap.get(src)).synonyms;
  boolean inc=false;
  for (  Token token : tokens) {
    if (exp.equals(new String(token.termBuffer(),0,token.termLength())))     inc=true;
  }
  assertTrue(inc);
}
