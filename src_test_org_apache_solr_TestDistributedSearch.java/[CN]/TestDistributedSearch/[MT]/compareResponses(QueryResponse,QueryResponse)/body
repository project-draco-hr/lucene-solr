{
  String cmp;
  System.out.println(a);
  System.out.println(b);
  cmp=compare(a.getResponse(),b.getResponse(),flags,handle);
  if (cmp != null) {
    System.out.println(a);
    System.out.println(b);
    TestCase.fail(cmp);
  }
}
