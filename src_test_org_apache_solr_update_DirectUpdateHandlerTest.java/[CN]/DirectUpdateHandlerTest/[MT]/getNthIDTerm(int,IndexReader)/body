{
  TermEnum te=r.terms(new Term("id",""));
  try {
    int x=0;
    do {
      if (x >= n) {
        return te.term().text();
      }
      x++;
    }
 while (te.next());
  }
  finally {
    te.close();
  }
  return null;
}
