{
  CategoryAttributesStream stream=new CategoryAttributesStream(categoryContainer);
  int nTokens;
  for (nTokens=0; stream.incrementToken(); nTokens++) {
  }
  assertEquals("Wrong number of tokens",3,nTokens);
}
