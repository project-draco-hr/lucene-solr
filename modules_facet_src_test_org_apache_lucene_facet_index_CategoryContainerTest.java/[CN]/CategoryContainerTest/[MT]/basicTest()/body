{
  assertEquals("Wrong number of categories in the container",3,categoryContainer.size());
  categoryContainer.clear();
  assertEquals("Container should not contain categories after clear",0,categoryContainer.size());
}
