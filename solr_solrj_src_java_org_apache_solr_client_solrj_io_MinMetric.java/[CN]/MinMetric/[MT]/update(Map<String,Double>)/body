{
  if (isDouble) {
    double dmin=metricValues.get(MIN);
    if (dmin < doubleMin) {
      doubleMin=dmin;
    }
  }
 else {
    double dmin=metricValues.get(MIN);
    long lmin=(long)dmin;
    if (lmin < longMin) {
      longMin=lmin;
    }
  }
}
