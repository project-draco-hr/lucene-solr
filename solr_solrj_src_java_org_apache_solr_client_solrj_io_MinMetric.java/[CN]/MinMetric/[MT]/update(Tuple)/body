{
  if (isDouble) {
    double d=(double)tuple.get(column);
    if (d < doubleMin) {
      doubleMin=d;
    }
  }
 else {
    long l=(long)tuple.get(column);
    if (l < longMin) {
      longMin=l;
    }
  }
}
