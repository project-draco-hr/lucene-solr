{
  return new DataSource<Iterator<Map<String,Object>>>(){
    @Override public Iterator<Map<String,Object>> getData(    String query){
      return rows.iterator();
    }
    @Override public void init(    Context context,    Properties initProps){
    }
    @Override public void close(){
    }
  }
;
}
