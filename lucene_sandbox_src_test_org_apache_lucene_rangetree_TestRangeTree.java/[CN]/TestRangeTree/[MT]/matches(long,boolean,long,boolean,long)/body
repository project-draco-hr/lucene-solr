{
  if (value > lower && value < upper) {
    return true;
  }
  if (value == lower && includeLower) {
    return true;
  }
  if (value == upper && includeUpper) {
    return true;
  }
  return false;
}
