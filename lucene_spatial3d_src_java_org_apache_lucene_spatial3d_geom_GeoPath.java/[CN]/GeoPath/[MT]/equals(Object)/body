{
  if (!(o instanceof GeoPath))   return false;
  GeoPath p=(GeoPath)o;
  if (!super.equals(p))   return false;
  if (cutoffAngle != p.cutoffAngle)   return false;
  return points.equals(p.points);
}
