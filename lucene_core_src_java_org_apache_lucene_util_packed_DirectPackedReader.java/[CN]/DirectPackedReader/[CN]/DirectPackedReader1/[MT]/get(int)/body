{
  try {
    in.seek(startPointer + (index >>> 3));
    int shift=7 - (index & 7);
    return (in.readByte() >>> shift) & 0x1;
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
}
