{
  if (rectCrosses(rMinX,rMinY,rMaxX,rMaxY,sMinX,sMinY,sMaxX,sMaxY) == true) {
    return false;
  }
  return !(rectCrossesPolyApprox(rMinX,rMinY,rMaxX,rMaxY,shapeX,shapeY,sMinX,sMinY,sMaxX,sMaxY) || !pointInPolygon(shapeX,shapeY,rMinY,rMinX));
}
