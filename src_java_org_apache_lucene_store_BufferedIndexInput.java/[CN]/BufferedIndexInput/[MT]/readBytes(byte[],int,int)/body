{
  if (len < BUFFER_SIZE) {
    for (int i=0; i < len; i++)     b[i + offset]=(byte)readByte();
  }
 else {
    long start=getFilePointer();
    seekInternal(start);
    readInternal(b,offset,len);
    bufferStart=start + len;
    bufferPosition=0;
    bufferLength=0;
  }
}
