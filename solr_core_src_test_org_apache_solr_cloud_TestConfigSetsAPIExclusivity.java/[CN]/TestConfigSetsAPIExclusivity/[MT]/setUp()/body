{
  super.setUp();
  final File solrXml=getFile("solr").toPath().resolve("solr.xml").toFile();
  final File testDir=createTempDir().toFile();
  solrCluster=new MiniSolrCloudCluster(1,testDir,solrXml,buildJettyConfig("/solr"));
}
