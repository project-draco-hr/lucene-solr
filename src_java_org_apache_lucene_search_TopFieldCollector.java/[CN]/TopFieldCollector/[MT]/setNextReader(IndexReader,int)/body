{
  final int numSlotsFull;
  if (queueFull)   numSlotsFull=numHits;
 else   numSlotsFull=totalHits;
  currentDocBase=docBase;
  for (int i=0; i < numComparators; i++) {
    comparators[i].setNextReader(reader,docBase,numSlotsFull);
  }
}
