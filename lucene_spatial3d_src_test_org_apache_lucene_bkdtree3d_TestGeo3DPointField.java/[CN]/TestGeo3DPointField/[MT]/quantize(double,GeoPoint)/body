{
  return new GeoPoint(decodeValueCenter(planetMax,encodeValue(planetMax,point.x)),decodeValueCenter(planetMax,encodeValue(planetMax,point.y)),decodeValueCenter(planetMax,encodeValue(planetMax,point.z)));
}
