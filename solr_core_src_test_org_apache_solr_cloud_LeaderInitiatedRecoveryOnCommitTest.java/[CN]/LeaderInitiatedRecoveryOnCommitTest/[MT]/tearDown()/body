{
  System.clearProperty("numShards");
  try {
    super.tearDown();
  }
 catch (  Exception exc) {
  }
  resetExceptionIgnores();
  if (!proxies.isEmpty()) {
    for (    SocketProxy proxy : proxies.values()) {
      proxy.close();
    }
  }
}
