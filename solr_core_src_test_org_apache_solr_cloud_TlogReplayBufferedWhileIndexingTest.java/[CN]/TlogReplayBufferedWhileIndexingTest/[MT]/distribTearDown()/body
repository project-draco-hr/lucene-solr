{
  if (threads != null) {
    for (    StoppableIndexingThread thread : threads) {
      thread.safeStop();
    }
    for (    StoppableIndexingThread thread : threads) {
      thread.join();
    }
  }
  super.distribTearDown();
}
