{
  int sz=fqList1.size();
  outer:   for (int i=start; i < sz; i++) {
    Query q1=fqList1.get(i);
    for (int j=start; j < sz; j++) {
      if (q1.equals(fqList2.get(j)))       continue outer;
    }
    return false;
  }
  return true;
}
