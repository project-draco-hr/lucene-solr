{
  long size=BASE_RAM_BYTES_USED + RamUsageEstimator.shallowSizeOf(blocks) + bytesUsedPerBlock * numBlocks;
  if (currentBlock != null) {
    size+=RamUsageEstimator.sizeOf(currentBlock);
  }
  return size;
}
