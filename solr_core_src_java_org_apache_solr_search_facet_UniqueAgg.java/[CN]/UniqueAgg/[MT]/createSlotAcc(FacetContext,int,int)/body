{
  if (fcontext.qcontext.searcher().getSchema().getField(getArg()).multiValued())   return new UniqueMultivaluedSlotAcc(fcontext,getArg(),numSlots);
 else   return new UniqueSinglevaluedSlotAcc(fcontext,getArg(),numSlots);
}
