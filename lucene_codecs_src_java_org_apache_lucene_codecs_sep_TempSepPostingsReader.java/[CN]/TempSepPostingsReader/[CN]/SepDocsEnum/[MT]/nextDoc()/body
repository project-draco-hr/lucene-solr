{
  while (true) {
    if (count == docFreq) {
      return doc=NO_MORE_DOCS;
    }
    count++;
    accum+=docReader.next();
    if (!omitTF) {
      freq=freqReader.next();
    }
    if (liveDocs == null || liveDocs.get(accum)) {
      break;
    }
  }
  return (doc=accum);
}
