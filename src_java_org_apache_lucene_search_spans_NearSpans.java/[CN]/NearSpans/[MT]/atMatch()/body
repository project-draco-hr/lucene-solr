{
  if (min().doc() == max.doc()) {
    int matchLength=max.end() - min().start();
    if (((matchLength - totalLength) <= slop) && (!inOrder || matchIsOrdered())) {
      return true;
    }
  }
  return false;
}
