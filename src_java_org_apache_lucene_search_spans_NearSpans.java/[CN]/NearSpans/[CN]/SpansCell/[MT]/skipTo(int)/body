{
  if (length != -1)   totalLength-=length;
  boolean more=spans.skipTo(target);
  if (more) {
    length=end() - start();
    totalLength+=length;
    if (max == null || doc() > max.doc() || (doc() == max.doc() && end() > max.end()))     max=this;
  }
  return more;
}
