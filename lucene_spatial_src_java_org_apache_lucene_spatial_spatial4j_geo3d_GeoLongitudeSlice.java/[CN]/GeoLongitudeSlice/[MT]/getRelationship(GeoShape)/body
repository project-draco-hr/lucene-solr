{
  if (path.isWithin(0.0,0.0,Math.PI * 0.5) && path.isWithin(0.0,0.0,-Math.PI * 0.5))   return CONTAINS;
  if (path.intersects(leftPlane,rightPlane) || path.intersects(rightPlane,leftPlane))   return OVERLAPS;
  if (isWithin(path.getInteriorPoint()))   return WITHIN;
  return DISJOINT;
}
