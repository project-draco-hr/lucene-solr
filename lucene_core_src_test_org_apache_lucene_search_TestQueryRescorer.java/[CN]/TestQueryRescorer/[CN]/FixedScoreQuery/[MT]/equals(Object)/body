{
  if ((o instanceof FixedScoreQuery) == false) {
    return false;
  }
  FixedScoreQuery other=(FixedScoreQuery)o;
  return super.equals(o) && reverse == other.reverse && Arrays.equals(idToNum,other.idToNum);
}
