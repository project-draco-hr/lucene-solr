{
  if ((o instanceof FixedScoreQuery) == false) {
    return false;
  }
  FixedScoreQuery other=(FixedScoreQuery)o;
  return Float.floatToIntBits(getBoost()) == Float.floatToIntBits(other.getBoost()) && reverse == other.reverse && Arrays.equals(idToNum,other.idToNum);
}
