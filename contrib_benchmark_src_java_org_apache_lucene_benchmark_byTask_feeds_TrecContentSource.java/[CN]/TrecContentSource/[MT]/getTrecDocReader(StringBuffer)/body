{
  StringBufferReader r=(StringBufferReader)trecDocReader.get();
  if (r == null) {
    r=new StringBufferReader(docBuffer);
    trecDocReader.set(r);
  }
 else {
    r.set(docBuffer);
  }
  return r;
}
