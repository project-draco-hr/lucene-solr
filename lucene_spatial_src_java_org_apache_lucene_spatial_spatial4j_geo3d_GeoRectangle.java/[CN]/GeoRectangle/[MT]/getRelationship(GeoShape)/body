{
  boolean ulhcWithin=path.isWithin(ULHC);
  boolean urhcWithin=path.isWithin(URHC);
  boolean lrhcWithin=path.isWithin(LRHC);
  boolean llhcWithin=path.isWithin(LLHC);
  if (ulhcWithin && urhcWithin && lrhcWithin&& llhcWithin) {
    return CONTAINS;
  }
  if (ulhcWithin || urhcWithin || lrhcWithin|| llhcWithin) {
    return OVERLAPS;
  }
  if (path.intersects(topPlane,bottomPlane,leftPlane,rightPlane) || path.intersects(bottomPlane,topPlane,leftPlane,rightPlane) || path.intersects(leftPlane,topPlane,bottomPlane,rightPlane)|| path.intersects(rightPlane,leftPlane,topPlane,bottomPlane))   return OVERLAPS;
  if (isWithin(path.getInteriorPoint()))   return WITHIN;
  return DISJOINT;
}
