{
  while (valueUpto < valueCount) {
    valuesIter.next();
    valueUpto++;
  }
  while (true) {
    docID++;
    if (docID == maxDoc) {
      docID=NO_MORE_DOCS;
      break;
    }
 else {
      valueCount=Math.toIntExact(valueCountsIter.next());
      if (valueCount > 0) {
        valueUpto=0;
        break;
      }
    }
  }
  return docID;
}
