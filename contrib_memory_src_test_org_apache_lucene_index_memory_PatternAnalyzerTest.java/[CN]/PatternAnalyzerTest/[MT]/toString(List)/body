{
  if (tokens == null)   return "null";
  String str="[";
  for (int i=0; i < tokens.size(); i++) {
    Token t1=(Token)tokens.get(i);
    str=str + "'" + t1.term()+ "', ";
  }
  return str + "]";
}
