{
  String stateFormat=System.getProperty("tests.solr.stateFormat",null);
  if (stateFormat != null) {
    if ("2".equals(stateFormat)) {
      return defaultStateFormat=2;
    }
 else     if ("1".equals(stateFormat)) {
      return defaultStateFormat=1;
    }
  }
  return defaultStateFormat;
}
