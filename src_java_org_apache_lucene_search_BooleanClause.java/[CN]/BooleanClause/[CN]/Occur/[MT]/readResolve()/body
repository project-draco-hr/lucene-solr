{
  int id=((Occur)this).id;
switch (id) {
case MUST_ID:
    return Occur.MUST;
case MUST_NOT_ID:
  return Occur.MUST_NOT;
case SHOULD_ID:
return Occur.SHOULD;
default :
throw new StreamCorruptedException("Unknown id " + id);
}
}
