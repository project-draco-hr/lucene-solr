{
  long costCandidateGeneration=0;
  for (int i=0; i < nrInHeap; i++)   costCandidateGeneration+=subScorers[i].cost();
  final float c1=1.0f, c2=1.0f;
  return (long)(c1 * costCandidateGeneration + c2 * costCandidateGeneration * (mm - 1));
}
