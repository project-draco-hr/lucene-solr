{
  Highlighter highlighter=new Highlighter(getFormatter(fieldName,request),getQueryScorer(query,fieldName,request));
  highlighter.setTextFragmenter(getFragmenter(fieldName,request));
  return highlighter;
}
