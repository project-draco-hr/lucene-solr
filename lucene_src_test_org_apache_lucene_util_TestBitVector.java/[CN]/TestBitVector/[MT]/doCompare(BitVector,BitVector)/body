{
  boolean equal=true;
  for (int i=0; i < bv.size(); i++) {
    if (bv.get(i) != compare.get(i)) {
      equal=false;
      break;
    }
  }
  assertEquals(bv.count(),compare.count());
  return equal;
}
