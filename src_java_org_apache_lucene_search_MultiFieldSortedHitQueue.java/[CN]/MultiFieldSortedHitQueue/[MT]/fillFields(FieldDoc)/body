{
  final int n=comparators.length;
  final Object[] fields=new Object[n];
  for (int i=0; i < n; ++i)   fields[i]=comparators[i].sortValue(doc);
  doc.fields=fields;
  if (maxscore > 1.0f)   doc.score/=maxscore;
  return doc;
}
