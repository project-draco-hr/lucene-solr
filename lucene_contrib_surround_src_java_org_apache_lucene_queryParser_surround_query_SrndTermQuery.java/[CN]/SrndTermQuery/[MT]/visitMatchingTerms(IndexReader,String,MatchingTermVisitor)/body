{
  TermEnum enumerator=reader.terms(getLuceneTerm(fieldName));
  try {
    Term it=enumerator.term();
    if ((it != null) && it.text().equals(getTermText()) && it.field().equals(fieldName)) {
      mtv.visitMatchingTerm(it);
    }
  }
  finally {
    enumerator.close();
  }
}
