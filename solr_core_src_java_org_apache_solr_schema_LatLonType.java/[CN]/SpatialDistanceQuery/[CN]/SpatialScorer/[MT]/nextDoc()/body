{
  for (; ; ) {
    ++doc;
    if (doc >= maxDoc) {
      return doc=NO_MORE_DOCS;
    }
    if (liveDocs != null && !liveDocs.get(doc))     continue;
    if (!match())     continue;
    return doc;
  }
}
