{
  if (sb.length() > 0) {
    char ch=sb.charAt(sb.length() - 1);
    if (ch == 'Y') {
      sb.setCharAt(sb.length() - 1,'i');
      setStrings();
    }
 else     if (ch == '?') {
      sb.setCharAt(sb.length() - 1,'c');
      setStrings();
    }
  }
}
