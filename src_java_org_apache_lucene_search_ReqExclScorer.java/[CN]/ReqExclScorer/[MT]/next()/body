{
  if (firstTime) {
    if (!exclDisi.next()) {
      exclDisi=null;
    }
    firstTime=false;
  }
  if (reqScorer == null) {
    return false;
  }
  if (!reqScorer.next()) {
    reqScorer=null;
    return false;
  }
  if (exclDisi == null) {
    return true;
  }
  return toNonExcluded();
}
