{
  if (module == null) {
    return false;
  }
  boolean removed=list.removeElement(module);
  if (removed) {
    Log.write(Log.CONN,"Conn:  Removed module " + module.getName() + " from "+ ((list == DefaultModuleList) ? "default " : "")+ "list");
  }
  return removed;
}
