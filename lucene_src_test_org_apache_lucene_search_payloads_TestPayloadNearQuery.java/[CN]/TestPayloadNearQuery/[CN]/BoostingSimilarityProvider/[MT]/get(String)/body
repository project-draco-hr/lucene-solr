{
  return new DefaultSimilarity(){
    @Override public float scorePayload(    int docId,    int start,    int end,    byte[] payload,    int offset,    int length){
      return payload[offset];
    }
    @Override public byte computeNorm(    FieldInvertState state){
      return encodeNormValue(state.getBoost());
    }
    @Override public float sloppyFreq(    int distance){
      return 1.0f;
    }
    @Override public float tf(    float freq){
      return 1.0f;
    }
    @Override public Explanation idfExplain(    TermContext states[],    IndexSearcher searcher) throws IOException {
      return new Explanation(1.0f,"Inexplicable");
    }
  }
;
}
