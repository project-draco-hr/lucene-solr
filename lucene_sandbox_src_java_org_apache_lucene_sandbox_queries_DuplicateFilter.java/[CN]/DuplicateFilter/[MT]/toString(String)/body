{
  return "DuplicateFilter(" + "fieldName=" + fieldName + ","+ "keepMode="+ (keepMode == KeepMode.KM_USE_FIRST_OCCURRENCE ? "first" : "last")+ ","+ "processingMode="+ (processingMode == ProcessingMode.PM_FAST_INVALIDATION ? "fast" : "full")+ ")";
}
