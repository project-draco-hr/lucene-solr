{
  if (CLUSTERSTATUS.isEqual(message.getStr(Overseer.QUEUE_OPERATION)))   return ExclusiveMarking.EXCLUSIVE;
  if (collectionWip.contains(collectionName))   return ExclusiveMarking.NONEXCLUSIVE;
  return ExclusiveMarking.NOTDETERMINED;
}
