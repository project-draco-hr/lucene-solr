{
  initAutomata(editDistance);
  if (automata != null && editDistance < automata.size()) {
    return new AutomatonFuzzyTermsEnum(automata.get(editDistance),term,reader,minSimilarity,runAutomata.subList(0,editDistance + 1).toArray(new RunAutomaton[0]),lastTerm);
  }
 else {
    return null;
  }
}
