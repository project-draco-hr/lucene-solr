{
  initAutomata(editDistance);
  if (runAutomata != null && editDistance < runAutomata.size()) {
    return new AutomatonFuzzyTermsEnum(runAutomata.subList(0,editDistance + 1).toArray(new ByteRunAutomaton[0]),lastTerm);
  }
 else {
    return null;
  }
}
