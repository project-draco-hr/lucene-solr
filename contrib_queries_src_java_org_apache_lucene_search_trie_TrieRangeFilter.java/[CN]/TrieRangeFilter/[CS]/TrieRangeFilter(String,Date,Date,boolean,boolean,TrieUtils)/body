{
  this(field,(min == null) ? null : variant.dateToTrieCoded(min),(max == null) ? null : variant.dateToTrieCoded(max),minInclusive,maxInclusive,variant);
  this.minUnconverted=min;
  this.maxUnconverted=max;
}
