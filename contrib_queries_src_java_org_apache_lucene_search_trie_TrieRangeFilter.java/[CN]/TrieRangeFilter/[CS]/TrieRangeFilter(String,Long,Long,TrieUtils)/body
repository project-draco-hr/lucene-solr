{
  this(field,(min == null) ? null : variant.longToTrieCoded(min.longValue()),(max == null) ? null : variant.longToTrieCoded(max.longValue()),variant);
  this.minUnconverted=min;
  this.maxUnconverted=max;
}
