{
  return ((Long)new SegmentInfos.FindSegmentsFile(directory){
    @Override public Object doBody(    String segmentFileName) throws IOException {
      return Long.valueOf(directory.fileModified(segmentFileName));
    }
  }
.run()).longValue();
}
