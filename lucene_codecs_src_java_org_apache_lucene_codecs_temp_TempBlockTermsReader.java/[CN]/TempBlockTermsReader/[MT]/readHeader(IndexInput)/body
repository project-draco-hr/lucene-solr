{
  int version=CodecUtil.checkHeader(input,TempBlockTermsWriter.CODEC_NAME,TempBlockTermsWriter.VERSION_START,TempBlockTermsWriter.VERSION_CURRENT);
  if (version < TempBlockTermsWriter.VERSION_APPEND_ONLY) {
    dirOffset=input.readLong();
  }
  return version;
}
