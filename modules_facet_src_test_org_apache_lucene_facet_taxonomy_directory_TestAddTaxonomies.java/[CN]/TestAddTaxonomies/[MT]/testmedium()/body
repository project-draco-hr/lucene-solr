{
  int numTests=atLeast(3);
  for (int i=0; i < numTests; i++) {
    dotest(_TestUtil.nextInt(random(),1,10),_TestUtil.nextInt(random(),1,100),_TestUtil.nextInt(random(),100,1000),random().nextBoolean());
  }
}
