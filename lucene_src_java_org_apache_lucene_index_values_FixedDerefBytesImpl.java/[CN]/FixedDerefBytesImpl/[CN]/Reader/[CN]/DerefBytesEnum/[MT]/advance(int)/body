{
  if (target < valueCount) {
    final long address=idx.advance(target);
    pos=idx.ord();
    if (address == 0) {
      ref.length=0;
      ref.offset=0;
      return pos;
    }
    fill(address,ref);
    return pos;
  }
  return pos=NO_MORE_DOCS;
}
