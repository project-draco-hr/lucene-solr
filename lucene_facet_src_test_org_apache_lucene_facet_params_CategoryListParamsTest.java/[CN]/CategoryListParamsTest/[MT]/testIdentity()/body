{
  CategoryListParams clParams1=new CategoryListParams();
  assertEquals("A CategoryListParams object does not equal itself.",clParams1,clParams1);
  assertEquals("A CategoryListParams object's hashCode does not equal itself.",clParams1.hashCode(),clParams1.hashCode());
}
