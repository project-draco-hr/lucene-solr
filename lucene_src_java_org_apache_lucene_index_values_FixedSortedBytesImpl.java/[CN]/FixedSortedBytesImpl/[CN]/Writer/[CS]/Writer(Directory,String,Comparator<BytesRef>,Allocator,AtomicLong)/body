{
  super(dir,id,CODEC_NAME,VERSION_CURRENT,true,new ByteBlockPool(allocator),bytesUsed,IOContext.DEFAULT);
  docToEntry=new int[1];
  bytesUsed.addAndGet(RamUsageEstimator.NUM_BYTES_INT);
  this.comp=comp;
}
