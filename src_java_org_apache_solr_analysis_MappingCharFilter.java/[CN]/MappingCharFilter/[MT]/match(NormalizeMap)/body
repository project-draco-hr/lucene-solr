{
  NormalizeMap result=null;
  if (map.submap != null) {
    int chr=nextChar();
    if (chr != -1) {
      NormalizeMap subMap=map.submap.get((char)chr);
      if (subMap != null) {
        result=match(subMap);
      }
      if (result == null)       pushChar(chr);
    }
  }
  if (result == null && map.normStr != null)   result=map;
  return result;
}
