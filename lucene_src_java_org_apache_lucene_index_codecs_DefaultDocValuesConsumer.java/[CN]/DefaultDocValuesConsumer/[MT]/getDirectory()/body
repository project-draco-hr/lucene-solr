{
  if (directory == null) {
    directory=new CompoundFileDirectory(mainDirectory,IndexFileNames.segmentFileName(segmentName,DOC_VALUES_SEGMENT_SUFFIX,IndexFileNames.COMPOUND_FILE_EXTENSION),context,true);
  }
  return directory;
}
