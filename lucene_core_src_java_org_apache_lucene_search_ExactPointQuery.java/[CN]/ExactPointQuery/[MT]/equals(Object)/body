{
  if (super.equals(other)) {
    final ExactPointQuery q=(ExactPointQuery)other;
    return q.numDims == numDims && q.bytesPerDim == bytesPerDim && Arrays.equals(point,q.point);
  }
  return false;
}
