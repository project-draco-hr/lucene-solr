{
  SortedMap<K,V> m=copy;
  if (m != null) {
    return m;
  }
  cloneLock.lock();
  try {
    m=copy;
    if (m != null) {
      return m;
    }
    SortedMap<K,V> clone=clone(delegate);
    copy=clone;
    return clone;
  }
  finally {
    cloneLock.unlock();
  }
}
