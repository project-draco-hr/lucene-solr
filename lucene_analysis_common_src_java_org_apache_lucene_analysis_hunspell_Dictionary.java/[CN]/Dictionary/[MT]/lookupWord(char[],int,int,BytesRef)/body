{
  Integer ord=null;
  try {
    ord=lookupOrd(word,offset,length);
  }
 catch (  IOException ex) {
  }
  if (ord == null) {
    return null;
  }
  return decodeFlags(flagLookup.get(ord,scratch));
}
