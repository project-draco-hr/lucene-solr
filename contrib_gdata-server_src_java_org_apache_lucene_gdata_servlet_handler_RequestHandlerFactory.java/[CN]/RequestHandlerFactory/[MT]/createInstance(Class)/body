{
  if (qualifiedClass == null)   throw new IllegalArgumentException("Factory class is null -- must be a implementation of org.apache.lucene.gdata.servlet.handler.RequestHandlerFactory");
  try {
    return (RequestHandlerFactory)qualifiedClass.newInstance();
  }
 catch (  Exception e) {
    FactoryImplementationException ex=new FactoryImplementationException("Factory implementation could not be created",e.getCause());
    ex.setStackTrace(e.getStackTrace());
    throw ex;
  }
}
