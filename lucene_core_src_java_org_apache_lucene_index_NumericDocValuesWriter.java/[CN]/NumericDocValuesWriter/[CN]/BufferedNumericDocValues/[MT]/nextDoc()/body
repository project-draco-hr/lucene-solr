{
  if (docID == size - 1) {
    docID=NO_MORE_DOCS;
  }
 else {
    int next=docsWithField.nextSetBit(docID + 1);
    if (next == NO_MORE_DOCS) {
      docID=NO_MORE_DOCS;
    }
 else {
      while (docID < next) {
        docID++;
        value=iter.next();
      }
    }
  }
  return docID;
}
