{
  if (ncomponents == 0) {
    return 0;
  }
  int hash=ncomponents;
  for (int i=0; i < ncomponents; i++) {
    hash=hash * 31 + ends[i];
  }
  int len=ends[ncomponents - 1];
  for (int i=0; i < len; i++) {
    hash=hash * 31 + chars[i];
  }
  return hash;
}
