{
  if (ncomponents == 0) {
    return "";
  }
  StringBuilder sb=new StringBuilder(ends[ncomponents - 1] + (ncomponents - 1));
  try {
    this.appendTo(sb,delimiter);
  }
 catch (  IOException e) {
  }
  return sb.toString();
}
