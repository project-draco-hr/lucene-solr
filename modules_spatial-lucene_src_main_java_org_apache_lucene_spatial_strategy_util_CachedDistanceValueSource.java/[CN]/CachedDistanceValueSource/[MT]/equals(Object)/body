{
  if (obj == null) {
    return false;
  }
  if (obj == this) {
    return true;
  }
  if (obj.getClass() != getClass()) {
    return false;
  }
  CachedDistanceValueSource rhs=(CachedDistanceValueSource)obj;
  return new EqualsBuilder().append(calculator,rhs.calculator).append(from,rhs.from).isEquals();
}
